---
title: list_doorbell_messages
description: "List doorbell cameras (those with ``isDoorbell=True``)."
draft: false
---

# `list_doorbell_messages`

List doorbell cameras (those with ``isDoorbell=True``).

Side effects: None (read-only).

Returns the subset of cameras that are doorbells. Useful as a
pre-step before configuring chime / message tools (future Phase 3.x
work). Each entry includes the camera's ``id``, ``name``, and
``isConnected``.

## Example

```python
list_doorbell_messages(controller="default")
```

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `controller` | `string` | no | "default" | Name of the UniFi controller to target. Defaults to ``"default"``. |

<!-- Generated by scripts/generate_tool_manifest.py. Do not edit by hand. -->
