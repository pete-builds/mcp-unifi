---
title: list_access_devices
description: "List hubs, readers, relays, and intercoms bonded to the controller."
draft: false
---

# `list_access_devices`

List hubs, readers, relays, and intercoms bonded to the controller.

Side effects: None (read-only).

Returns one record per device with ``id``, ``type`` (``hub`` or
``reader``), ``model``, ``name``, ``mac``, ``ip``, ``online``,
``firmware_version``, and (for readers) the ``door_id`` and
``hub_id`` they belong to.

## Example

```python
list_access_devices(controller="default")
```

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `controller` | `string` | no | "default" | Name of the UniFi controller to target. |

<!-- Generated by scripts/generate_tool_manifest.py. Do not edit by hand. -->
