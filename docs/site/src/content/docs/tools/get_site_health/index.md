---
title: get_site_health
description: "Report per-subsystem health (wan, lan, wlan, www, vpn)."
draft: false
---

# `get_site_health`

Report per-subsystem health (wan, lan, wlan, www, vpn).

Side effects: None (read-only).

Returns one record per subsystem with ``subsystem``, ``status``, and
subsystem-specific metrics (e.g. WAN throughput, LAN client counts).

## Example

```python
get_site_health(controller="default")
```

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `controller` | `string` | no | "default" | Name of the UniFi controller to target. Defaults to ``"default"``. |

<!-- Generated by scripts/generate_tool_manifest.py. Do not edit by hand. -->
