---
title: list_access_users
description: "List every user enrolled in the Access system."
draft: false
---

# `list_access_users`

List every user enrolled in the Access system.

Side effects: None (read-only).

Returns one record per user with ``id``, ``first_name``, ``last_name``,
``full_name``, ``email``, ``status``, ``employee_number``, and
``credential_ids`` (cross-reference with ``list_credentials``).

## Example

```python
list_access_users(controller="default")
```

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `controller` | `string` | no | "default" | Name of the UniFi controller to target. |

<!-- Generated by scripts/generate_tool_manifest.py. Do not edit by hand. -->
