---
title: list_traffic_rules
description: "List v2 traffic rules (app/domain/IP-based allow & block policies)."
draft: false
---

# `list_traffic_rules`

List v2 traffic rules (app/domain/IP-based allow & block policies).

Side effects: None (read-only).

Returns one record per rule with ``_id``, ``action``
(``BLOCK``/``ALLOW``), ``matching_target``, ``target_devices``, and
``enabled``. Empty on a gateway with no traffic rules configured.

## Example

```python
list_traffic_rules(controller="default")
```

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `controller` | `string` | no | "default" | Name of the UniFi controller to target. Defaults to ``"default"``. |

<!-- Generated by scripts/generate_tool_manifest.py. Do not edit by hand. -->
