---
title: list_port_forwards
description: "List every port-forward (DNAT) rule on the controller."
draft: false
---

# `list_port_forwards`

List every port-forward (DNAT) rule on the controller.

Side effects: None (read-only).

Returns one record per forward with ``_id``, ``name``, ``enabled``,
``proto``, ``src``, ``fwd``, ``fwd_port``, and ``dst_port``.

## Example

```python
list_port_forwards(controller="default")
```

## Parameters

| Parameter | Type | Required | Default | Description |
|---|---|---|---|---|
| `controller` | `string` | no | "default" | Name of the UniFi controller to target. Defaults to ``"default"``. |

<!-- Generated by scripts/generate_tool_manifest.py. Do not edit by hand. -->
