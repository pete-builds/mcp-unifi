{
  "tool_count": 129,
  "tools": [
    {
      "description": "Return credentials expiring within ``days_ahead`` days.\n\nSide effects: None (read-only).\n\nIterates ``list_credentials`` client-side and filters to those whose\n``expires_at`` falls inside the window ``[now, now + days_ahead]``.\nCredentials with no ``expires_at`` (typical for mobile credentials)\nare excluded. Each entry includes ``days_until_expiry`` so the LLM\ncan sort and summarise without re-doing the math.\n\nExample: audit_expiring_credentials(days_ahead=14)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "credential_type": {
            "default": "",
            "description": "Optional ``\"nfc\"``, ``\"pin\"``, or ``\"mobile\"``\nfilter. Empty (default) returns every type.",
            "type": "string"
          },
          "days_ahead": {
            "default": 30,
            "description": "Look-ahead window in days. Defaults to 30.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "audit_expiring_credentials"
    },
    {
      "description": "Compare current controller state to a declared YAML spec.\n\nRead-only \u2014 never mutates the controller. Returns a structured diff\nshowing fields that drifted, resources missing from the controller, and\nresources present on the controller that the spec did not declare.\n\nSide effects:\n- None (read-only). Lists networks, WLANs, and firewall rules.\n\nSpec format (YAML, all sections optional):\n\n    networks:\n      - name: \"IoT\"\n        vlan: 50\n        subnet: \"10.50.0.0/24\"\n    wlans:\n      - name: \"Cameras-IoT\"\n        network: \"IoT\"        # references a network by name\n        security: \"wpapsk\"\n    firewall_rules:\n      - name: \"Block IoT to LAN\"\n        action: \"drop\"\n        src: \"10.50.0.0/24\"\n        dst: \"192.168.86.0/24\"\n\nResources are matched by ``name`` (case-insensitive). Sections you omit\nare not audited; sections you include audit BOTH directions (missing\nand extra). To audit a section as \"exactly these resources\", include it\nexplicitly. To audit as \"at least these resources\", omit the section\nand use ``audit_open_ports`` or other read-only tools instead.\n\nReturns ``{\"in_sync\": bool, \"controller\": str, \"summary\": str,\n\"drifts\": [...]}``. Each drift is ``{\"resource_type\", \"name\",\n\"field\", \"expected\", \"actual\"}``. The synthetic field ``_resource``\nflags presence/absence of an entire resource (``expected=null`` =\nextra; ``actual=null`` = missing).\n\nExample: audit_network_drift(spec_yaml=\"networks:\\n  - name: iot\\n    vlan: 50\\n\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "spec_yaml": {
            "description": "The spec document, as a YAML string.",
            "type": "string"
          }
        },
        "required": [
          "spec_yaml"
        ],
        "type": "object"
      },
      "name": "audit_network_drift"
    },
    {
      "description": "Audit WAN-facing exposure (port forwards and WAN_IN accept rules).\n\nSide effects: None (read-only).\n\nCross-references firewall rules and port forwards to summarise what\nis reachable from the public internet:\n- Active port forwards (DNAT into the LAN).\n- WAN_IN ``accept`` rules, excluding the boilerplate\n  established/related rule.\n\nUseful as a \"did I leave something open?\" sanity check before\npublishing a service or shipping a config.\n\nExample: audit_open_ports(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "audit_open_ports"
    },
    {
      "description": "Snapshot every persistent resource on the controller into one envelope.\n\nSide effects: None (read-only).\n\nCaptures networks/VLANs, WLANs, firewall rules, port profiles,\nstatic DHCP reservations, and port forwards into one versioned JSON\nenvelope. Transient state (clients, devices, observability) is\nexcluded.\n\nSecret handling: every WLAN's ``x_passphrase`` is replaced with the\nsentinel ``<redacted-on-backup>``. The envelope flag\n``secrets_stripped: true`` warns ``restore_config`` to force\nrestored WLANs to ``enabled=False``.\n\nReturns the JSON envelope ``{\"schema\": \"1\", \"controller\": str,\n\"ts\": iso8601, \"secrets_stripped\": bool, \"resources\": {...}}``.\n\nExample: backup_config(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "backup_config"
    },
    {
      "description": "Block a client by MAC so it cannot rejoin until unblocked.\n\nSide effects:\n- Adds the MAC to the controller's user-block list. The client is\n  immediately disconnected and prevented from re-associating on any\n  SSID until ``unblock_client`` is called.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: block_client(mac=\"aa:bb:cc:00:00:01\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "mac": {
            "description": "Client MAC address (e.g. ``\"aa:bb:cc:00:00:01\"``).",
            "type": "string"
          }
        },
        "required": [
          "mac"
        ],
        "type": "object"
      },
      "name": "block_client"
    },
    {
      "description": "Execute a queued destructive action by its preview token.\n\nSide effects:\n- Runs the mutation that was previewed (delete VLAN, WLAN, firewall\n  rule, port profile, port forward, or static DHCP lease).\n- Removes the token from the in-process pending-actions registry.\n  A second call with the same token returns an error.\n\nExample: confirm_destructive_action(token=\"3f1a...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "token": {
            "description": "The ``token`` field from a previous destructive tool's\npreview envelope. Tokens expire 5 minutes after issuance.",
            "type": "string"
          }
        },
        "required": [
          "token"
        ],
        "type": "object"
      },
      "name": "confirm_destructive_action"
    },
    {
      "description": "Create a Dynamic DNS update configuration.\n\nSide effects:\n- Adds a DDNS config: the gateway will push the WAN IP to ``service``\n  for ``host_name`` whenever the WAN address changes. Takes effect\n  immediately (the gateway sends an update on the next IP check).\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: create_dynamic_dns(service=\"namecheap\", host_name=\"home.example.com\", login=\"example.com\", password=\"<token>\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set (with the password redacted).",
            "type": "boolean"
          },
          "enabled": {
            "default": true,
            "description": "``False`` creates the config disabled for staging.",
            "type": "boolean"
          },
          "host_name": {
            "description": "The FQDN to keep pointed at the WAN IP\n(``\"home.example.com\"``).",
            "type": "string"
          },
          "interface": {
            "default": "wan",
            "description": "WAN interface whose IP is tracked. Defaults to\n``\"wan\"`` (the primary WAN on a single-WAN gateway).",
            "type": "string"
          },
          "login": {
            "description": "Provider account / username. For some providers (e.g.\nNamecheap) this is the zone/domain.",
            "type": "string"
          },
          "password": {
            "description": "Provider password or API token. Stored write-only and\nredacted in reads.",
            "type": "string"
          },
          "server": {
            "default": "",
            "description": "Custom update-URL host. Only used (and required) when\n``service == \"custom\"``; leave empty otherwise.",
            "type": "string"
          },
          "service": {
            "description": "Provider keyword (e.g. ``\"dyndns\"``, ``\"namecheap\"``,\n``\"cloudflare\"``, ``\"noip\"``, or ``\"custom\"``).",
            "type": "string"
          }
        },
        "required": [
          "service",
          "host_name",
          "login",
          "password"
        ],
        "type": "object"
      },
      "name": "create_dynamic_dns"
    },
    {
      "description": "Create a reusable firewall group of addresses, IPv6 addresses, or ports.\n\nSide effects:\n- Adds a new reusable object. It does nothing on its own until a\n  firewall rule or policy references it by ``_id``.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: create_firewall_group(name=\"IoT Subnets\", group_type=\"address-group\", members=[\"10.50.0.0/24\", \"10.60.0.0/24\"])",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "group_type": {
            "description": "One of ``\"address-group\"`` (IPv4 CIDRs/IPs),\n``\"ipv6-address-group\"`` (IPv6 CIDRs/IPs), or ``\"port-group\"``\n(TCP/UDP port numbers and ranges).",
            "type": "string"
          },
          "members": {
            "description": "The group's members as strings. For address groups:\nCIDRs or IPs (``\"10.50.0.0/24\"``). For IPv6 address groups:\nIPv6 CIDRs/IPs. For port groups: ports or ranges\n(``\"443\"``, ``\"8000-8100\"``).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "name": {
            "description": "Display name for the group (e.g. ``\"IoT Subnets\"``).",
            "type": "string"
          }
        },
        "required": [
          "name",
          "group_type",
          "members"
        ],
        "type": "object"
      },
      "name": "create_firewall_group"
    },
    {
      "description": "Create a firewall rule on the controller.\n\nSide effects:\n- Adds a new rule into the named ``ruleset`` at the given\n  ``rule_index``. Rules with lower indexes evaluate first.\n- Takes effect immediately on the next packet hitting the affected\n  datapath.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: create_firewall_rule(name=\"Allow IoT to Plex\", ruleset=\"LAN_IN\", action=\"accept\", protocol=\"tcp\", src_networkconf_id=\"65f...\", dst_networkconf_id=\"65a...\", dst_port=\"32400\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "action": {
            "description": "``\"accept\"``, ``\"drop\"``, or ``\"reject\"``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "dst_address": {
            "default": "",
            "description": "Destination CIDR. Empty = any.",
            "type": "string"
          },
          "dst_networkconf_id": {
            "default": "",
            "description": "Destination network ``_id``. Use this OR\n``dst_address``.",
            "type": "string"
          },
          "dst_networkconf_type": {
            "default": "NETv4",
            "description": "Discriminator that pairs with\n``dst_networkconf_id``. Same semantics as\n``src_networkconf_type``. Defaults to ``\"NETv4\"``.",
            "type": "string"
          },
          "dst_port": {
            "default": "",
            "description": "Destination port match. Same syntax as ``src_port``.\nThe headline use case: ``dst_port=\"32400\"`` with\n``protocol=\"tcp\"`` to allow IoT\u2192Plex without opening the\nrest of MGMT.",
            "type": "string"
          },
          "enabled": {
            "default": true,
            "description": "``False`` creates the rule disabled for staging.",
            "type": "boolean"
          },
          "name": {
            "description": "Display name for the rule (e.g. ``\"Block iot to LAN\"``).",
            "type": "string"
          },
          "protocol": {
            "default": "all",
            "description": "``\"all\"``, ``\"tcp\"``, ``\"udp\"``, ``\"icmp\"``, etc.\nPort matches require ``\"tcp\"`` or ``\"udp\"``.",
            "type": "string"
          },
          "rule_index": {
            "default": 20000,
            "description": "Evaluation order. Lower = evaluated first. On UniFi\nNetwork 9.x (Zone-Based Firewall), user-defined LAN_IN rules\nlive at ``20000`` and above; lower bands are reserved by the\ncontroller and will fail with\n``api.err.FirewallRuleIndexOutOfRange``. ``20000`` is the safe\ndefault. (Older controllers used 2000-3999.)",
            "type": "integer"
          },
          "ruleset": {
            "description": "Where the rule is enforced. Common values: ``\"LAN_IN\"``,\n``\"LAN_OUT\"``, ``\"LAN_LOCAL\"``, ``\"WAN_IN\"``, ``\"WAN_OUT\"``,\n``\"WAN_LOCAL\"``, ``\"GUEST_IN\"``, ``\"GUEST_OUT\"``,\n``\"GUEST_LOCAL\"``.",
            "type": "string"
          },
          "src_address": {
            "default": "",
            "description": "Source CIDR (e.g. ``\"10.50.0.0/24\"``). Empty = any.",
            "type": "string"
          },
          "src_networkconf_id": {
            "default": "",
            "description": "Source network ``_id``. Use this OR\n``src_address``.",
            "type": "string"
          },
          "src_networkconf_type": {
            "default": "NETv4",
            "description": "Discriminator that pairs with\n``src_networkconf_id``. UniFi Network 9.x ZBF requires this\nwhenever a rule references a network conf by ``_id`` and\nreturns ``api.err.FirewallRuleNetworkConfTypeRequired``\notherwise. Defaults to ``\"NETv4\"`` (IPv4 network). Only\nemitted to the controller when ``src_networkconf_id`` is set.",
            "type": "string"
          },
          "src_port": {
            "default": "",
            "description": "Source port match. Single port (``\"443\"``), CSV\n(``\"80,443\"``), or range (``\"3000-3100\"``). Empty = any.\nRequires ``protocol`` set to ``\"tcp\"`` or ``\"udp\"``.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "ruleset",
          "action"
        ],
        "type": "object"
      },
      "name": "create_firewall_rule"
    },
    {
      "description": "Provision an isolated guest network end-to-end: VLAN + guest SSID + drop rule.\n\nSide effects:\n- Step 1: creates a VLAN-tagged network with ``purpose=\"guest\"`` at\n  the given ``subnet`` (or ``10.0.<vlan_id>.0/24`` by default).\n- Step 2: creates a guest WPA2 WiFi SSID (client isolation enabled)\n  bound to the new network. Access points start broadcasting within\n  seconds.\n- Step 3: creates a LAN_IN drop rule blocking the guest subnet from\n  reaching ``main_lan_subnet``.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n- Rollback: if any sub-step fails, all prior sub-steps are reverted\n  (firewall_rule \u2192 wlan \u2192 network) and the response includes\n  ``rolled_back`` and ``partial`` keys.\n\nExample: create_guest_network(name=\"guest\", ssid=\"guest-wifi\", passphrase=\"hunter2hunter2\", vlan_id=60, main_lan_subnet=\"192.168.1.0/24\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "hide_ssid": {
            "default": false,
            "description": "``True`` suppresses SSID broadcast (rare for guest).",
            "type": "boolean"
          },
          "main_lan_subnet": {
            "default": "192.168.1.0/24",
            "description": "CIDR of the main LAN. The drop rule blocks\nguest \u2192 this subnet.",
            "type": "string"
          },
          "name": {
            "description": "Display name for the network record (e.g. ``\"guest\"``).",
            "type": "string"
          },
          "passphrase": {
            "description": "WPA2 PSK (8-63 chars).",
            "type": "string"
          },
          "schedule": {
            "default": "",
            "description": "Optional schedule descriptor (controller field\n``schedule``). Empty = always on.",
            "type": "string"
          },
          "ssid": {
            "description": "SSID to broadcast (e.g. ``\"guest-wifi\"``).",
            "type": "string"
          },
          "subnet": {
            "default": "",
            "description": "Override the guest subnet. Empty uses\n``IOT_SUBNET_TEMPLATE`` (default ``10.0.{vlan_id}.0/24``).",
            "type": "string"
          },
          "vlan_id": {
            "description": "802.1Q VLAN ID, 2-4094.",
            "type": "integer"
          }
        },
        "required": [
          "name",
          "ssid",
          "passphrase",
          "vlan_id"
        ],
        "type": "object"
      },
      "name": "create_guest_network"
    },
    {
      "description": "Add a honeypot trap to a network.\n\nSide effects:\n- Reads the current ``ips`` setting, appends a new entry\n  ``{\"network_id\": ..., \"ip_address\": ..., \"version\": \"v4\"}``,\n  and writes the updated list back via ``POST /set/setting/ips``.\n- Sets ``honeypot_enabled=true`` on the controller (the toggle\n  tracks list non-emptiness).\n- Mutates controller state. Use dry_run=True to preview.\n\nRefuses to write if the (network_id, ip) pair is already a\nhoneypot or if ``ip`` is not a valid LAN IPv4 address.\n\nExample: create_honeypot(network_id=\"65f...\", ip=\"10.0.50.2\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it.",
            "type": "boolean"
          },
          "ip": {
            "description": "Unallocated IPv4 address inside the target network.",
            "type": "string"
          },
          "network_id": {
            "description": "The ``_id`` from ``list_networks`` for the VLAN the\nhoneypot should live on.",
            "type": "string"
          }
        },
        "required": [
          "network_id",
          "ip"
        ],
        "type": "object"
      },
      "name": "create_honeypot"
    },
    {
      "description": "Provision an isolated IoT network end-to-end: VLAN + SSID + drop rule.\n\nSide effects:\n- Step 1: creates a VLAN-tagged network at the given ``subnet`` (or\n  ``10.0.<vlan_id>.0/24`` by default).\n- Step 2: creates a WPA2 WiFi SSID bound to the new network. Access\n  points start broadcasting within seconds.\n- Step 3 (when ``isolate=True``): creates a LAN_IN drop rule blocking\n  the IoT subnet from reaching ``main_lan_subnet``.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n- Rollback: if any sub-step fails, all prior sub-steps are reverted\n  (firewall_rule \u2192 wlan \u2192 network) and the response includes\n  ``rolled_back`` and ``partial`` keys.\n\nExample: create_iot_network(name=\"iot\", vlan_id=50, passphrase=\"hunter2hunter2\", main_lan_subnet=\"192.168.1.0/24\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "hide_ssid": {
            "default": false,
            "description": "``True`` suppresses SSID broadcast.",
            "type": "boolean"
          },
          "isolate": {
            "default": true,
            "description": "``True`` (default) creates the LAN_IN drop rule. ``False``\nlets IoT devices reach the main LAN (rare).",
            "type": "boolean"
          },
          "main_lan_subnet": {
            "default": "192.168.1.0/24",
            "description": "CIDR of the main/trusted LAN. The isolation rule\nblocks IoT \u2192 this subnet. Defaults to ``\"192.168.1.0/24\"``.",
            "type": "string"
          },
          "name": {
            "description": "Used for both the network name and the SSID (e.g. ``\"iot\"``).",
            "type": "string"
          },
          "passphrase": {
            "description": "WPA2 PSK for the IoT SSID (8-63 chars).",
            "type": "string"
          },
          "subnet": {
            "default": "",
            "description": "Override the IoT subnet. Empty uses\n``IOT_SUBNET_TEMPLATE`` (default ``10.0.{vlan_id}.0/24``).",
            "type": "string"
          },
          "vlan_id": {
            "description": "802.1Q VLAN ID, 2-4094. Also drives the default subnet.",
            "type": "integer"
          }
        },
        "required": [
          "name",
          "vlan_id",
          "passphrase"
        ],
        "type": "object"
      },
      "name": "create_iot_network"
    },
    {
      "description": "Create a port-forward (DNAT) rule on the WAN.\n\nSide effects:\n- Adds a NAT rule that exposes the internal host ``fwd:fwd_port`` to\n  the WAN on ``dst_port``. The service is reachable from the public\n  internet (subject to ``src`` restriction).\n- Takes effect immediately on the next inbound packet.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: create_port_forward(name=\"plex\", fwd=\"10.50.0.10\", fwd_port=\"32400\", dst_port=\"32400\", proto=\"tcp\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "dst_port": {
            "description": "External / WAN port to listen on.",
            "type": "string"
          },
          "enabled": {
            "default": true,
            "description": "``True`` enables the rule immediately.",
            "type": "boolean"
          },
          "fwd": {
            "description": "Internal IP to forward to (e.g. ``\"10.50.0.10\"``).",
            "type": "string"
          },
          "fwd_port": {
            "description": "Internal port (string; UniFi accepts ranges like\n``\"8000-8010\"``).",
            "type": "string"
          },
          "log": {
            "default": false,
            "description": "``True`` logs forwarded packets.",
            "type": "boolean"
          },
          "name": {
            "description": "Display name for the rule.",
            "type": "string"
          },
          "proto": {
            "default": "tcp",
            "description": "``\"tcp\"``, ``\"udp\"``, or ``\"tcp_udp\"``.",
            "type": "string"
          },
          "src": {
            "default": "any",
            "description": "Source restriction. ``\"any\"`` (default) or a CIDR.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "fwd",
          "fwd_port",
          "dst_port"
        ],
        "type": "object"
      },
      "name": "create_port_forward"
    },
    {
      "description": "Create a switch port profile.\n\nSide effects:\n- Adds a new profile defining native (untagged) VLAN, tagged VLANs,\n  PoE behaviour, and forwarding mode. The profile is dormant until\n  a switch port is assigned to it (via ``set_port_state``).\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: create_port_profile(name=\"iot-trunk\", native_networkconf_id=\"65f...\", tagged_networkconf_ids=[\"65a...\", \"65b...\"], poe_mode=\"auto\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "forward": {
            "default": "all",
            "description": "``\"all\"`` (default), ``\"native\"``, ``\"customize\"``, or\n``\"disabled\"``.",
            "type": "string"
          },
          "name": {
            "description": "Display name for the profile (e.g. ``\"iot-trunk\"``).",
            "type": "string"
          },
          "native_networkconf_id": {
            "default": "",
            "description": "``_id`` of the native (untagged) network.\nEmpty for trunk-only ports.",
            "type": "string"
          },
          "poe_mode": {
            "default": "auto",
            "description": "``\"auto\"`` (default), ``\"passive24v\"``,\n``\"passthrough\"``, or ``\"off\"``.",
            "type": "string"
          },
          "tagged_networkconf_ids": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Optional list of network ``_id`` strings\ncarried as tagged VLANs."
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "name": "create_port_profile"
    },
    {
      "description": "Create a static next-hop route.\n\nSide effects:\n- Adds a destination-based route: traffic to ``destination`` is sent\n  to ``next_hop`` instead of following the default route. Takes effect\n  immediately on the gateway's routing table.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nBLAST RADIUS: a static route changes where traffic for the\ndestination prefix is sent. A wrong next-hop can black-hole that\nprefix. It does not affect traffic outside ``destination``.\n\nExample: create_route(name=\"Lab via firewall\", destination=\"10.99.0.0/24\", next_hop=\"192.168.1.254\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "destination": {
            "description": "Destination network in CIDR form\n(``\"10.99.0.0/24\"``). This is the prefix the route matches.",
            "type": "string"
          },
          "distance": {
            "default": 1,
            "description": "Administrative distance (1-255). Lower wins when two\nroutes match the same destination. Defaults to ``1``.",
            "type": "integer"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "enabled": {
            "default": true,
            "description": "``False`` creates the route disabled for staging.",
            "type": "boolean"
          },
          "name": {
            "description": "Display name for the route (e.g. ``\"Lab via firewall\"``).",
            "type": "string"
          },
          "next_hop": {
            "description": "Next-hop gateway IP that traffic to ``destination`` is\nforwarded to (must be reachable on a directly-connected\nnetwork).",
            "type": "string"
          }
        },
        "required": [
          "name",
          "destination",
          "next_hop"
        ],
        "type": "object"
      },
      "name": "create_route"
    },
    {
      "description": "Reserve a fixed IP for a client by MAC.\n\nSide effects:\n- Adds a fixed-IP entry on the user object so ``mac`` always receives\n  ``ip`` from the controller's DHCP server. The next DHCP renewal\n  for that client picks up the reservation.\n- The IP must fall inside the network's subnet or the controller\n  will reject the request.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: create_static_dhcp_lease(mac=\"aa:bb:cc:00:00:01\", ip=\"10.50.0.10\", network_id=\"65f...\", name=\"cameras-nvr\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "hostname": {
            "default": "",
            "description": "DHCP hostname (optional).",
            "type": "string"
          },
          "ip": {
            "description": "IPv4 address to reserve.",
            "type": "string"
          },
          "mac": {
            "description": "Client MAC address (e.g. ``\"aa:bb:cc:00:00:01\"``).",
            "type": "string"
          },
          "name": {
            "default": "",
            "description": "Friendly display name (optional).",
            "type": "string"
          },
          "network_id": {
            "description": "``_id`` of the network/VLAN this client lives on.",
            "type": "string"
          }
        },
        "required": [
          "mac",
          "ip",
          "network_id"
        ],
        "type": "object"
      },
      "name": "create_static_dhcp_lease"
    },
    {
      "description": "Create a v2 traffic rule from a full rule object.\n\nSide effects:\n- Adds a new app/domain/IP allow-or-block policy that takes effect\n  immediately on matching traffic.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nThe v2 traffic-rule schema is rich and version-dependent, so this tool\ntakes the full rule object as a dict rather than fixed parameters.\nBuild it from a ``get_traffic_rule_details`` example on a similar rule,\nor from the UniFi UI's network inspector.\n\nExample: create_traffic_rule(rule={\"action\": \"BLOCK\", \"matching_target\": \"INTERNET\", \"target_devices\": [], \"enabled\": True})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "rule": {
            "additionalProperties": true,
            "description": "The full traffic-rule object. Common keys: ``action``\n(``\"BLOCK\"``/``\"ALLOW\"``), ``matching_target``\n(``\"INTERNET\"``/``\"DOMAIN\"``/``\"IP\"``/``\"REGION\"`` etc.),\n``target_devices`` (list), ``enabled`` (bool), and the\ntarget-specific fields (``domains``, ``ip_addresses``,\n``app_ids``, ``regions``) for the chosen ``matching_target``.",
            "type": "object"
          }
        },
        "required": [
          "rule"
        ],
        "type": "object"
      },
      "name": "create_traffic_rule"
    },
    {
      "description": "Create a VLAN-tagged network on the controller.\n\nSide effects:\n- Adds a new network record with the given VLAN ID, IP subnet, and\n  DHCP scope. The first usable address (.1) becomes the gateway.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: create_vlan(name=\"iot\", vlan_id=50, subnet=\"10.50.0.0/24\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dhcp_start": {
            "default": "",
            "description": "First DHCP lease address. Empty = derived from\n``IOT_DHCP_START_OFFSET``.",
            "type": "string"
          },
          "dhcp_stop": {
            "default": "",
            "description": "Last DHCP lease address. Empty = derived from\n``IOT_DHCP_STOP_OFFSET``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "name": {
            "description": "Network display name (e.g. ``\"iot\"``, ``\"cameras\"``).",
            "type": "string"
          },
          "purpose": {
            "default": "corporate",
            "description": "``\"corporate\"`` for normal LANs, ``\"guest\"`` for\nhotspot-style isolation.",
            "type": "string"
          },
          "subnet": {
            "description": "Subnet in either gateway-IP form (``\"10.50.0.1/24\"``,\nwhat the UniFi controller stores) or network form\n(``\"10.50.0.0/24\"``). Network form is auto-promoted to\ngateway form before the POST so callers don't have to\nremember which one UniFi wants. /24 only.",
            "type": "string"
          },
          "vlan_id": {
            "description": "802.1Q VLAN ID, 2-4094.",
            "type": "integer"
          }
        },
        "required": [
          "name",
          "vlan_id",
          "subnet"
        ],
        "type": "object"
      },
      "name": "create_vlan"
    },
    {
      "description": "Create a WiFi SSID bound to an existing network/VLAN.\n\nSide effects:\n- Adds a new WLAN record. Access points start broadcasting the SSID\n  within seconds of the apply.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nAP-group binding:\n- UniFi controllers reject ``POST /rest/wlanconf`` with\n  ``api.err.ApGroupMissing`` when ``ap_group_ids`` is absent. When\n  ``ap_group_ids`` is unset, this tool calls ``list_ap_groups`` and\n  uses the controller's \"default\" group automatically. Pass an\n  explicit list to broadcast only on specific groups.\n\nExample: create_wlan(name=\"iot\", passphrase=\"hunter2hunter2\", network_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "ap_group_ids": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "List of AP group ``_id`` strings to broadcast on.\nEmpty/None auto-resolves to the controller's \"default\"\ngroup via ``list_ap_groups``."
          },
          "ap_group_mode": {
            "default": "all",
            "description": "``\"all\"`` (default) broadcasts on every AP in the\nlisted groups. Matches the controller UI default.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "hide_ssid": {
            "default": false,
            "description": "``True`` suppresses SSID broadcast.",
            "type": "boolean"
          },
          "is_guest": {
            "default": false,
            "description": "``True`` isolates clients from each other and the rest\nof the LAN.",
            "type": "boolean"
          },
          "name": {
            "description": "SSID broadcast name (e.g. ``\"iot\"``).",
            "type": "string"
          },
          "network_id": {
            "description": "``_id`` of the network/VLAN this SSID lives on. Get\nit from ``list_networks``.",
            "type": "string"
          },
          "passphrase": {
            "description": "WPA pre-shared key (8-63 chars). Required unless\n``security=\"open\"``.",
            "type": "string"
          },
          "security": {
            "default": "wpapsk",
            "description": "``\"wpapsk\"`` (default), ``\"wpaeap\"``, or ``\"open\"``.",
            "type": "string"
          },
          "wlan_band": {
            "default": "both",
            "description": "``\"2g\"``, ``\"5g\"``, ``\"6g\"``, or ``\"both\"`` (default).",
            "type": "string"
          },
          "wpa_mode": {
            "default": "wpa2",
            "description": "``\"wpa2\"`` (default) or ``\"wpa3\"`` if all clients\nsupport it.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "passphrase",
          "network_id"
        ],
        "type": "object"
      },
      "name": "create_wlan"
    },
    {
      "description": "Preview deletion of a content-filtering profile.\n\nThis tool does not delete on its own. It returns a preview envelope\nwith a ``token``; call ``confirm_destructive_action(token)`` to commit\nthe delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the profile. The clients and networks in its\n  scope stop being filtered by it and fall back to whatever other\n  profile (or none) applies.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token: purely informational, no commit step possible.\n\nExample: delete_content_filter(filter_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "filter_id": {
            "description": "The ``_id`` from ``list_content_filters``.",
            "type": "string"
          }
        },
        "required": [
          "filter_id"
        ],
        "type": "object"
      },
      "name": "delete_content_filter"
    },
    {
      "description": "Preview deletion of a Dynamic DNS config.\n\nThis tool does not delete on its own. It returns a preview envelope\nwith a ``token``; call ``confirm_destructive_action(token)`` to commit\nthe delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the DDNS config. The provider stops receiving\n  updates from the gateway; the external hostname goes stale once its\n  TTL lapses.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token: purely informational, no commit step possible.\n\nExample: delete_dynamic_dns(ddns_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "ddns_id": {
            "description": "The ``_id`` from ``list_dynamic_dns``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          }
        },
        "required": [
          "ddns_id"
        ],
        "type": "object"
      },
      "name": "delete_dynamic_dns"
    },
    {
      "description": "Preview deletion of a firewall group.\n\nThis tool no longer deletes on its own. It returns a preview envelope\nwith a ``token``; call ``confirm_destructive_action(token)`` to commit\nthe delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the group. **The controller rejects deletion of\n  a group still referenced by any firewall rule** \u2014 detach it from\n  every rule first.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token \u2014 purely informational, no commit step possible.\n\nExample: delete_firewall_group(group_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "group_id": {
            "description": "The ``_id`` from ``list_firewall_groups``.",
            "type": "string"
          }
        },
        "required": [
          "group_id"
        ],
        "type": "object"
      },
      "name": "delete_firewall_group"
    },
    {
      "description": "Preview deletion of a firewall rule.\n\nv0.7.0: this tool no longer deletes on its own. It returns a preview\nenvelope with a ``token``; call ``confirm_destructive_action(token)``\nto commit the delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the rule. Traffic that previously matched it\n  falls through to the next rule (or the implicit default).\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token \u2014 purely informational, no commit step possible.\n\nExample: delete_firewall_rule(rule_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "rule_id": {
            "description": "The ``_id`` from ``list_firewall_rules``.",
            "type": "string"
          }
        },
        "required": [
          "rule_id"
        ],
        "type": "object"
      },
      "name": "delete_firewall_rule"
    },
    {
      "description": "Preview deletion of a honeypot entry.\n\nv0.7.0 destructive pattern: this tool does not delete on its own.\nIt returns a preview envelope with a ``token``; call\n``confirm_destructive_action(token)`` to commit. Tokens expire\nafter 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: rewrites the ``ips`` setting's ``honeypot`` list with\n  the entry removed and sets ``honeypot_enabled`` based on whether\n  the resulting list is empty.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope\n  with no token (informational only).\n\nExample: delete_honeypot(id=\"65f...:10.0.50.2\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "id": {
            "description": "The synthesised honeypot id from ``list_honeypots`` in\n``{network_id}:{ip}`` form.",
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "type": "object"
      },
      "name": "delete_honeypot"
    },
    {
      "description": "Preview deletion of a port-forward (DNAT) rule.\n\nv0.7.0: this tool no longer deletes on its own. It returns a preview\nenvelope with a ``token``; call ``confirm_destructive_action(token)``\nto commit the delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the NAT rule. The internal service stops\n  being reachable from the WAN immediately.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token \u2014 purely informational, no commit step possible.\n\nExample: delete_port_forward(forward_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "forward_id": {
            "description": "The ``_id`` from ``list_port_forwards``.",
            "type": "string"
          }
        },
        "required": [
          "forward_id"
        ],
        "type": "object"
      },
      "name": "delete_port_forward"
    },
    {
      "description": "Preview deletion of a switch port profile.\n\nv0.7.0: this tool no longer deletes on its own. It returns a preview\nenvelope with a ``token``; call ``confirm_destructive_action(token)``\nto commit the delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the profile. The controller rejects the\n  request if any switch port still references it.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token \u2014 purely informational, no commit step possible.\n\nExample: delete_port_profile(profile_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "profile_id": {
            "description": "The ``_id`` from ``list_port_profiles``.",
            "type": "string"
          }
        },
        "required": [
          "profile_id"
        ],
        "type": "object"
      },
      "name": "delete_port_profile"
    },
    {
      "description": "Preview deletion of a static route.\n\nThis tool no longer deletes on its own. It returns a preview envelope\nwith a ``token``; call ``confirm_destructive_action(token)`` to commit\nthe delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the route. Traffic to the destination prefix\n  falls back to the default route (or another matching route).\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token \u2014 purely informational, no commit step possible.\n\nExample: delete_route(route_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "route_id": {
            "description": "The ``_id`` from ``list_routes``.",
            "type": "string"
          }
        },
        "required": [
          "route_id"
        ],
        "type": "object"
      },
      "name": "delete_route"
    },
    {
      "description": "Preview deletion of a static DHCP reservation.\n\nv0.7.0: this tool no longer deletes on its own. It returns a preview\nenvelope with a ``token``; call ``confirm_destructive_action(token)``\nto commit the delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the fixed-IP entry. The client returns to\n  dynamic DHCP on its next renewal and may receive a different\n  address.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token \u2014 purely informational, no commit step possible.\n\nExample: delete_static_dhcp_lease(lease_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "lease_id": {
            "description": "The ``_id`` from ``list_dhcp_leases``.",
            "type": "string"
          }
        },
        "required": [
          "lease_id"
        ],
        "type": "object"
      },
      "name": "delete_static_dhcp_lease"
    },
    {
      "description": "Preview deletion of a VLAN/network.\n\nv0.7.0: this tool no longer deletes on its own. It returns a preview\nenvelope with a ``token``; call ``confirm_destructive_action(token)``\nto commit the delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the network record. Any WLANs, firewall rules,\n  or DHCP reservations still referencing it must be detached first\n  or the controller will reject the request. Clients on this VLAN\n  lose connectivity.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token \u2014 purely informational, no commit step possible.\n\nExample: delete_vlan(network_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "network_id": {
            "description": "The ``_id`` from ``list_networks``.",
            "type": "string"
          }
        },
        "required": [
          "network_id"
        ],
        "type": "object"
      },
      "name": "delete_vlan"
    },
    {
      "description": "Preview deletion of a WiFi SSID.\n\nv0.7.0: this tool no longer deletes on its own. It returns a preview\nenvelope with a ``token``; call ``confirm_destructive_action(token)``\nto commit the delete. Tokens expire after 5 minutes.\n\nSide effects:\n- None until ``confirm_destructive_action`` runs against the token.\n- On confirm: removes the WLAN record. APs stop broadcasting the\n  SSID within seconds. Connected wireless clients on this SSID are\n  immediately disconnected.\n- ``dry_run=True`` returns the legacy ``would_delete`` envelope with\n  no token \u2014 purely informational, no commit step possible.\n\nExample: delete_wlan(wlan_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "``True`` skips token generation and returns the legacy\n``{\"dry_run\": true, ...}`` envelope. ``False`` (default)\ngenerates a preview token that must be confirmed.",
            "type": "boolean"
          },
          "wlan_id": {
            "description": "The ``_id`` from ``list_wlans``.",
            "type": "string"
          }
        },
        "required": [
          "wlan_id"
        ],
        "type": "object"
      },
      "name": "delete_wlan"
    },
    {
      "description": "Fetch one Access device's full record by ID.\n\nSide effects: None (read-only).\n\nSame shape as one entry from ``list_access_devices``. Useful for\nchecking firmware version, online status, or hub assignment on a\nspecific reader.\n\nExample: get_access_device(device_id=\"dev...\", controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "device_id": {
            "description": "Device ``id`` from ``list_access_devices``.",
            "type": "string"
          }
        },
        "required": [
          "device_id"
        ],
        "type": "object"
      },
      "name": "get_access_device"
    },
    {
      "description": "Fetch one access policy's full record by ID.\n\nSide effects: None (read-only).\n\nSame shape as one entry from ``list_access_policies``. Useful when\nauditing why a specific user can or cannot badge into a specific door\nat a specific time.\n\nExample: get_access_policy(policy_id=\"pol...\", controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "policy_id": {
            "description": "Access policy ``id`` from ``list_access_policies``.",
            "type": "string"
          }
        },
        "required": [
          "policy_id"
        ],
        "type": "object"
      },
      "name": "get_access_policy"
    },
    {
      "description": "Return controller version, license tier, and capacity.\n\nSide effects: None (read-only).\n\nReturns ``version``, ``release_channel``, ``license`` (with\n``doors_used``, ``doors_max``, ``users_used``, ``users_max``),\n``controller_id``, ``uptime_seconds``. Useful for surfacing licence\nceilings before suggesting enrolling more doors / users.\n\nExample: get_access_system_info(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_access_system_info"
    },
    {
      "description": "List client-impacting anomalies the controller has detected.\n\nSide effects: None (read-only).\n\nReturns one record per anomaly with ``anomaly`` (an enum string such\nas ``USER_HIGH_TCP_LATENCY`` or ``USER_DNS_LATENCY``), ``mac`` (the\naffected client), and ``timestamps`` (a list of epoch-ms occurrence\ntimes). Returns an empty list on a clean network.\n\nExample: get_anomalies(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_anomalies"
    },
    {
      "description": "Fetch a single camera's full record by ID.\n\nSide effects: None (read-only).\n\nReturns the full camera record (same shape as one entry from\n``list_cameras``) including current recording mode, motion sensitivity,\nprivacy-mask state, and the most recent ``lastMotion`` timestamp.\n\nExample: get_camera(camera_id=\"65f...\", controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "description": "The Protect ``id`` (or ``_id``) from ``list_cameras``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "required": [
          "camera_id"
        ],
        "type": "object"
      },
      "name": "get_camera"
    },
    {
      "description": "List recent client connection sessions over a time window.\n\nSide effects: None (read-only).\n\nReturns one record per association, newest first, with ``mac``,\n``hostname``, ``name``, ``ip``, ``assoc_time`` (epoch seconds),\n``duration`` (seconds), ``rx_bytes``/``tx_bytes``, ``is_wired``,\n``is_guest``, ``ap_mac``, and ``satisfaction``. Pass ``mac`` to scope\nto one client; omit it for every client's sessions in the window.\n\nExample: get_client_sessions(mac=\"aa:bb:cc:00:00:01\", hours=48, limit=20)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "hours": {
            "default": 24,
            "description": "Look-back window in hours (default 24, max 720 = 30 days).",
            "type": "integer"
          },
          "limit": {
            "default": 50,
            "description": "Max number of sessions to return (default 50, max 1000).",
            "type": "integer"
          },
          "mac": {
            "default": "",
            "description": "MAC address to filter to one client. Empty (default) returns\nsessions for all clients.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_client_sessions"
    },
    {
      "description": "Report per-client traffic, signal, and uptime for one client by MAC.\n\nSide effects: None (read-only).\n\nReturns a record with ``hostname``, ``name``, ``ip``, ``is_wired``,\n``network``, ``essid``, ``uptime``, ``first_seen``/``last_seen``,\n``signal``/``rssi``/``satisfaction`` (wireless), ``tx_rate``/``rx_rate``,\n``tx_bytes``/``rx_bytes``, ``tx_retries``, ``anomalies``, and the wired\n``wired_rate_mbps``/``wired_tx_bytes``/``wired_rx_bytes`` fields when\npresent. Returns a NOT_FOUND error envelope if the client is not\ncurrently connected.\n\nExample: get_client_stats(mac=\"aa:bb:cc:00:00:01\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "mac": {
            "description": "MAC address of the connected client.",
            "type": "string"
          }
        },
        "required": [
          "mac"
        ],
        "type": "object"
      },
      "name": "get_client_stats"
    },
    {
      "description": "Show one content-filtering profile's full record by ``_id``.\n\nSide effects: None (read-only). Call this before\n``update_content_filter`` to see the profile's current categories,\nallow/block lists, and scope (the values you are about to\nread-modify-write).\n\nReturns the profile record, or an error envelope if no profile\nmatches.\n\nExample: get_content_filter_details(filter_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "filter_id": {
            "description": "The ``_id`` from ``list_content_filters``.",
            "type": "string"
          }
        },
        "required": [
          "filter_id"
        ],
        "type": "object"
      },
      "name": "get_content_filter_details"
    },
    {
      "description": "Fetch one credential's full record by ID.\n\nSide effects: None (read-only).\n\nSame shape as one entry from ``list_credentials`` plus any\ntype-specific fields (``card_id`` for NFC, ``pin_length`` for PIN,\n``device_label`` for mobile).\n\nExample: get_credential(credential_id=\"cred...\", controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "credential_id": {
            "description": "Credential ``id`` from ``list_credentials``.",
            "type": "string"
          }
        },
        "required": [
          "credential_id"
        ],
        "type": "object"
      },
      "name": "get_credential"
    },
    {
      "description": "Show per-radio RF settings for an access point.\n\nSide effects: None (read-only). Use this before any\n``set_radio_*`` call to see the current values you are about to\nchange.\n\nReturns the device identity plus one record per radio with\n``radio`` (UniFi id: ``ng``=2.4GHz, ``na``=5GHz, ``6e``=6GHz), the\nfriendly ``band``, ``channel``, ``ht`` (channel width MHz),\n``tx_power_mode`` (``auto`` when unset), ``tx_power``,\n``min_rssi_enabled``, ``min_rssi``, and the hardware\n``min_txpower``/``max_txpower`` bounds.\n\nExample: get_device_radios(device_mac=\"aa:bb:cc:00:00:02\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "device_mac": {
            "description": "AP MAC address (from ``list_devices``).",
            "type": "string"
          }
        },
        "required": [
          "device_mac"
        ],
        "type": "object"
      },
      "name": "get_device_radios"
    },
    {
      "description": "Report per-device stats for one UniFi device by MAC.\n\nSide effects: None (read-only).\n\nReturns a record with ``name``, ``model``, ``type``, ``state``,\n``uptime`` (seconds), ``cpu_pct``, ``mem_pct``, ``satisfaction``,\n``num_sta``, ``tx_bytes``/``rx_bytes``, and (for access points)\n``tx_retries``/``tx_packets``/``rx_packets``. Returns a NOT_FOUND\nerror envelope if no adopted device matches ``mac``.\n\nExample: get_device_stats(mac=\"f4:e2:c6:00:00:02\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "mac": {
            "description": "MAC address of the device (gateway, AP, or switch).",
            "type": "string"
          }
        },
        "required": [
          "mac"
        ],
        "type": "object"
      },
      "name": "get_device_stats"
    },
    {
      "description": "Fetch one door's full record by ID.\n\nSide effects: None (read-only).\n\nReturns the same fields as one entry in ``list_doors`` plus the live\nlock and position status. Useful for confirming a door is online\nbefore reading recent events against it.\n\nExample: get_door(door_id=\"abc...\", controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "door_id": {
            "description": "Access door ``id`` from ``list_doors``.",
            "type": "string"
          }
        },
        "required": [
          "door_id"
        ],
        "type": "object"
      },
      "name": "get_door"
    },
    {
      "description": "Show one Dynamic DNS config's full record by ``_id``.\n\nSide effects: None (read-only). Call this before\n``update_dynamic_dns`` to see the current provider, hostname, and\ntracked interface.\n\nReturns the DDNS record (with ``x_password`` redacted), or an error\nenvelope if no config matches.\n\nExample: get_dynamic_dns_details(ddns_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "ddns_id": {
            "description": "The ``_id`` from ``list_dynamic_dns``.",
            "type": "string"
          }
        },
        "required": [
          "ddns_id"
        ],
        "type": "object"
      },
      "name": "get_dynamic_dns_details"
    },
    {
      "description": "Fetch the JPEG thumbnail for a Protect event, base64-encoded.\n\nSide effects: None (read-only).\n\nSame response shape as ``get_snapshot``, but keyed on the event\ninstead of the camera. Useful for previewing a motion or\nsmart-detection trigger without pulling the full clip.\n\nExample: get_event_thumbnail(event_id=\"evt123\", controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "event_id": {
            "description": "Protect event ``id`` from ``list_motion_events`` or\n``list_smart_detections``.",
            "type": "string"
          }
        },
        "required": [
          "event_id"
        ],
        "type": "object"
      },
      "name": "get_event_thumbnail"
    },
    {
      "description": "Show one firewall group's full record by ``_id``.\n\nSide effects: None (read-only). Call this before\n``update_firewall_group`` to see the current members you are about to\nread-modify-write.\n\nReturns the group record with ``_id``, ``name``, ``group_type``, and\n``group_members``, or an error envelope if no group matches.\n\nExample: get_firewall_group_details(group_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "group_id": {
            "description": "The ``_id`` from ``list_firewall_groups``.",
            "type": "string"
          }
        },
        "required": [
          "group_id"
        ],
        "type": "object"
      },
      "name": "get_firewall_group_details"
    },
    {
      "description": "Report gateway resource stats: CPU, memory, temperature, throughput.\n\nSide effects: None (read-only).\n\nReturns a single record for the gateway (UCG/UDM) with ``cpu_pct``,\n``mem_pct``, ``temperatures`` (a list of ``{name, type, value}`` in\ndegrees C), ``uptime`` (seconds), ``tx_bytes``/``rx_bytes``,\n``num_sta``, ``last_wan_ip``, and ``speedtest_status``. Returns ``{}``\nif no gateway device is adopted on this controller.\n\nExample: get_gateway_stats(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_gateway_stats"
    },
    {
      "description": "Show one network's full record, grouped into readable sections.\n\nSide effects: None (read-only). This is the deep view that complements\n``list_networks`` (the summary). Use it to inspect a network's DHCP\nscope, IPv6 configuration, and VPN fields before editing with\n``update_vlan`` / ``set_lan_ipv6``.\n\nResolve the network by ``network_id`` (preferred, from\n``list_networks``) or by ``name`` (case-insensitive; errors if the name\nis ambiguous). Returns:\n\n- ``network``: identity fields (``_id``, ``name``, ``purpose``,\n  ``vlan``, ``vlan_enabled``, ``ip_subnet``, ``enabled``,\n  ``networkgroup``).\n- ``dhcp``: every ``dhcpd_*`` key (lease window, gateway/DNS toggles,\n  lease time, NTP/WINS) plus the IPv6 DHCP (``dhcpdv6_*``) keys.\n- ``ipv6``: the LAN IPv6 view: ``ipv6_interface_type``,\n  ``ipv6_ra_enabled``, ``ipv6_client_address_assignment``,\n  ``ipv6_pd_start`` / ``ipv6_pd_stop`` (the delegated-prefix window),\n  and the RA tuning keys. Empty section on a network with no IPv6.\n- ``vpn``: VPN-related keys (``vpn_type``, ``*_vpn_*``,\n  ``remote_vpn_*``) when the network is a VPN; empty otherwise.\n- ``raw``: the complete unfiltered record, so nothing is hidden.\n\nExample: get_network_details(name=\"Default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "name": {
            "default": "",
            "description": "Network display name (case-insensitive) as an alternative to\n``network_id``. Errors if more than one network matches.",
            "type": "string"
          },
          "network_id": {
            "default": "",
            "description": "The ``_id`` from ``list_networks``. Wins over ``name``\nwhen both are given.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_network_details"
    },
    {
      "description": "Return the most recent N events across every door.\n\nSide effects: None (read-only).\n\nConvenience wrapper around ``list_access_events`` with a 24-hour\nwindow and no filters. Useful for \"what just happened\" triage.\n\nExample: get_recent_access_events(limit=10)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "limit": {
            "default": 20,
            "description": "Maximum events returned. Defaults to 20.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "get_recent_access_events"
    },
    {
      "description": "Show one static route's full record by ``_id``.\n\nSide effects: None (read-only). Call this before ``update_route`` to\nsee the current destination, next hop, and distance.\n\nReturns the route record, or an error envelope if no route matches.\n\nExample: get_route_details(route_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "route_id": {
            "description": "The ``_id`` from ``list_routes``.",
            "type": "string"
          }
        },
        "required": [
          "route_id"
        ],
        "type": "object"
      },
      "name": "get_route_details"
    },
    {
      "description": "Report per-subsystem health (wan, lan, wlan, www, vpn).\n\nSide effects: None (read-only).\n\nReturns one record per subsystem with ``subsystem``, ``status``, and\nsubsystem-specific metrics (e.g. WAN throughput, LAN client counts).\n\nExample: get_site_health(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_site_health"
    },
    {
      "description": "Fetch a current JPEG snapshot from a camera, base64-encoded.\n\nSide effects: None (read-only).\n\nReturns ``{\"camera_id\": ..., \"format\": \"jpeg\", \"data\": \"<base64>\",\n\"size_bytes\": ...}``. The JPEG payload is base64-encoded so it can\nride inside the JSON envelope all MCP tools return; decode with\n``base64.b64decode`` to get the raw image bytes.\n\nExample: get_snapshot(camera_id=\"65f...\", controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "description": "Protect camera ``id``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "required": [
          "camera_id"
        ],
        "type": "object"
      },
      "name": "get_snapshot"
    },
    {
      "description": "List recent speed-test results, newest first.\n\nSide effects: None (read-only).\n\nReturns one record per test with ``time``, ``xput_up``,\n``xput_download``, ``latency``, and ``server``.\n\nExample: get_speedtest_results(limit=10)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "limit": {
            "default": 10,
            "description": "Max number of results to return (default 10, max 1000).",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "get_speedtest_results"
    },
    {
      "description": "Report controller/system info: version, uptime, device type.\n\nSide effects: None (read-only).\n\nReturns a single record with ``version``, ``build``,\n``previous_version``, ``hostname``, ``name``, ``uptime`` (seconds),\n``timezone``, ``ubnt_device_type``, ``udm_version``,\n``console_display_version``, and ``update_available``. Use it to check\nthe firmware level and whether an update is pending.\n\nExample: get_system_info(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_system_info"
    },
    {
      "description": "Return Teleport VPN state and any wireguard-server networks.\n\nSide effects: None (read-only).\n\nReturns ``{\"enabled\": bool, \"vpn_networks\": [...],\n\"clients_via_local_api\": false, \"notes\": [...]}``. The ``enabled``\nflag is the global Teleport listener toggle; ``vpn_networks``\nlists the underlying wireguard-server network records (Teleport\nrides on these for tunnel termination); ``clients_via_local_api``\nis a hard ``false`` on UniFi Network 9.x \u2014 the client roster and\ninvitation lifecycle are only reachable via the Ubiquiti mobile\napp pairing flow, not the local API.\n\nExample: get_teleport_config(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_teleport_config"
    },
    {
      "description": "Return current Threat Management (IDS/IPS) configuration.\n\nSide effects: None (read-only).\n\nReads the controller's ``ips`` setting record and projects it to the\noperator view: ``enabled`` (true when mode is ``ids`` or ``ips``),\n``mode`` (one of ``off`` / ``ids`` / ``ips``),\n``enabled_signature_categories`` (the IDS/IPS Emerging Threats rule\ngroups that are turned on), ``enabled_networks`` (network IDs the\nengine inspects), plus the adjacent booleans for endpoint scanning,\nad blocking, DNS filtering, and honeypot.\n\nExample: get_threat_management(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_threat_management"
    },
    {
      "description": "Show one traffic route's full record by ``_id``.\n\nSide effects: None (read-only). Call this before\n``update_traffic_route`` or ``toggle_traffic_route`` to see the route's\ncurrent matching target, next hop, and kill-switch state.\n\nReturns the route record, or an error envelope if no route matches.\n\nExample: get_traffic_route_details(route_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "route_id": {
            "description": "The ``_id`` from ``list_traffic_routes``.",
            "type": "string"
          }
        },
        "required": [
          "route_id"
        ],
        "type": "object"
      },
      "name": "get_traffic_route_details"
    },
    {
      "description": "Show one traffic rule's full record by ``_id``.\n\nSide effects: None (read-only). Call this before\n``update_traffic_rule`` or ``toggle_traffic_rule`` to see the rule's\ncurrent matching target and action.\n\nReturns the rule record, or an error envelope if no rule matches.\n\nExample: get_traffic_rule_details(rule_id=\"65f...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "rule_id": {
            "description": "The ``_id`` from ``list_traffic_rules``.",
            "type": "string"
          }
        },
        "required": [
          "rule_id"
        ],
        "type": "object"
      },
      "name": "get_traffic_rule_details"
    },
    {
      "description": "Fetch one visitor pass's full record by ID.\n\nSide effects: None (read-only).\n\nSame shape as one entry from ``list_visitors`` plus any per-pass\npurpose / notes.\n\nExample: get_visitor(visitor_id=\"vis...\", controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "visitor_id": {
            "description": "Visitor ``id`` from ``list_visitors``.",
            "type": "string"
          }
        },
        "required": [
          "visitor_id"
        ],
        "type": "object"
      },
      "name": "get_visitor"
    },
    {
      "description": "Show the current IPv6 configuration of the WAN uplink(s).\n\nSide effects: None (read-only). Call this before ``set_wan_ipv6`` to\nsee exactly what you are about to change.\n\nReturns one record per WAN interface with ``name``, ``_id``, and the\nIPv6 keys: ``wan_type_v6`` (connection type: ``disabled``/``dhcpv6``/\n``pppoe``/``static``), ``ipv6_wan_delegation_type`` (``none`` or\n``prefix-delegation``), ``wan_dhcpv6_pd_size_auto``,\n``wan_dhcpv6_pd_size`` (present only when PD is configured),\n``wan_ipv6_dns_preference``, and ``ipv6_setting_preference``.\n\nExample: get_wan_ipv6(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "wan_name": {
            "default": "",
            "description": "Restrict the view to a single WAN by display name\n(e.g. ``\"Internet 1\"``). Empty (default) returns every WAN.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_wan_ipv6"
    },
    {
      "description": "Report current WAN status: link, ISP, public IP, throughput, latency.\n\nSide effects: None (read-only).\n\nConvenience wrapper around ``get_site_health`` that returns just the\nWAN subsystem record. Returns ``{\"subsystem\": \"wan\", \"status\":\n\"unknown\"}`` if the controller does not report WAN.\n\nExample: get_wan_status(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "get_wan_status"
    },
    {
      "description": "List hubs, readers, relays, and intercoms bonded to the controller.\n\nSide effects: None (read-only).\n\nReturns one record per device with ``id``, ``type`` (``hub`` or\n``reader``), ``model``, ``name``, ``mac``, ``ip``, ``online``,\n``firmware_version``, and (for readers) the ``door_id`` and\n``hub_id`` they belong to.\n\nExample: list_access_devices(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_access_devices"
    },
    {
      "description": "List badge-scan / door events with filters.\n\nSide effects: None (read-only).\n\nReturns events whose timestamp falls inside the last ``hours_back``\nhours. ``result`` filters to ``\"granted\"`` or ``\"denied\"``; empty\nreturns both. ``door_id`` narrows to one door.\n\nExample: list_access_events(hours_back=6, result=\"denied\", limit=20)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "door_id": {
            "default": "",
            "description": "Optional door ``id`` filter. Empty returns events\nacross every door.",
            "type": "string"
          },
          "hours_back": {
            "default": 24,
            "description": "Look-back window in hours. Defaults to 24.",
            "type": "integer"
          },
          "limit": {
            "default": 100,
            "description": "Maximum events returned. Defaults to 100.",
            "type": "integer"
          },
          "result": {
            "default": "",
            "description": "Optional ``\"granted\"`` or ``\"denied\"`` filter. Empty\n(default) returns both.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_access_events"
    },
    {
      "description": "List every access policy on the controller.\n\nSide effects: None (read-only).\n\nEach policy ties a schedule (e.g. business hours), one or more door\ngroups, and a set of users / credential types. Returns ``id``,\n``name``, ``type``, ``schedule``, ``door_group_ids``, ``user_ids``,\n``credential_types``, ``active``.\n\nExample: list_access_policies(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_access_policies"
    },
    {
      "description": "List every user enrolled in the Access system.\n\nSide effects: None (read-only).\n\nReturns one record per user with ``id``, ``first_name``, ``last_name``,\n``full_name``, ``email``, ``status``, ``employee_number``, and\n``credential_ids`` (cross-reference with ``list_credentials``).\n\nExample: list_access_users(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_access_users"
    },
    {
      "description": "List controller alarms, active or archived.\n\nSide effects: None (read-only).\n\nReturns the most recent ``limit`` alarm records.\n\nExample: list_alarms(limit=20, archived=False)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "archived": {
            "default": false,
            "description": "``True`` lists dismissed/archived alarms; ``False``\n(default) lists active alarms only.",
            "type": "boolean"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "limit": {
            "default": 50,
            "description": "Max number of alarms to return (default 50, max 1000).",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "list_alarms"
    },
    {
      "description": "List access-point groups configured on the controller.\n\nSide effects: None (read-only).\n\nReturns one record per AP group with ``_id``, ``name``,\n``attr_hidden_id`` (the built-in \"default\" group carries\n``\"default\"`` here), ``device_macs``, and ``site_id``.\n\nUsed by ``create_wlan`` to auto-resolve the default AP group when no\nexplicit ``ap_group_ids`` is supplied. Call this directly to inspect\nwhich groups exist before creating per-AP-group WLANs.\n\nExample: list_ap_groups(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_ap_groups"
    },
    {
      "description": "List every camera bonded to the Protect controller.\n\nSide effects: None (read-only).\n\nReturns one record per camera with ``id``, ``name``, ``model``,\n``mac``, ``host``, ``state``, ``isConnected``, ``isDoorbell``, plus\nthe nested ``recordingSettings``, ``motionSettings``, ``privacyMask``,\nand ``stats`` blocks the UniFi Protect API surfaces.\n\nExample: list_cameras(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_cameras"
    },
    {
      "description": "List currently active wireless and wired clients.\n\nSide effects: None (read-only).\n\nMirrors the controller's Insights \u2192 Clients view: MAC, hostname, IP,\nnetwork, signal/satisfaction (wireless only), AP or switch port\n(wired), uptime, and last_seen.\n\nExample: list_clients(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_clients"
    },
    {
      "description": "List DNS content-filtering profiles (category blocking, safe-search).\n\nSide effects: None (read-only).\n\nReturns one record per profile with ``_id``, ``name``, ``enabled``,\n``categories`` (blocked DNS-category enum list), ``allow_list`` /\n``block_list`` (per-domain overrides), ``client_macs`` and\n``network_ids`` (the scope the profile applies to), ``safe_search``,\nand ``schedule``. Empty on a gateway with no filtering profiles.\n\nExample: list_content_filters(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_content_filters"
    },
    {
      "description": "List every credential enrolled in the Access controller.\n\nSide effects: None (read-only).\n\nReturns one record per credential with ``id``, ``type`` (``nfc``,\n``pin``, or ``mobile``), ``label``, ``user_id``, ``status``,\n``issued_at`` (ms), and ``expires_at`` (ms or null for non-expiring\nmobile credentials).\n\nExample: list_credentials(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_credentials"
    },
    {
      "description": "List every UniFi device adopted by this controller.\n\nSide effects: None (read-only).\n\nReturns one record per device (gateway, AP, switch) with ``_id``,\n``mac``, ``type``, ``model``, ``name``, ``ip``, ``version``,\n``state``, ``uptime``, ``num_sta``, and ``satisfaction``.\n\nExample: list_devices(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_devices"
    },
    {
      "description": "List static DHCP reservations on the controller.\n\nSide effects: None (read-only).\n\nUniFi stores fixed leases on the user object with\n``use_fixedip=true``. This returns just those entries with ``_id``,\n``mac``, ``name``, ``hostname``, ``fixed_ip``, and ``network_id``.\n\nExample: list_dhcp_leases(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_dhcp_leases"
    },
    {
      "description": "List every door group defined on the controller.\n\nSide effects: None (read-only).\n\nDoor groups bundle doors for access-policy assignment. Returns\n``id``, ``name``, ``door_ids``, and ``description``.\n\nExample: list_door_groups(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_door_groups"
    },
    {
      "description": "List doorbell cameras (those with ``isDoorbell=True``).\n\nSide effects: None (read-only).\n\nReturns the subset of cameras that are doorbells. Useful as a\npre-step before configuring chime / message tools (future Phase 3.x\nwork). Each entry includes the camera's ``id``, ``name``, and\n``isConnected``.\n\nExample: list_doorbell_messages(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_doorbell_messages"
    },
    {
      "description": "List every door bonded to the Access controller.\n\nSide effects: None (read-only).\n\nReturns one record per door with ``id``, ``name``, ``location``,\n``hub_id``, ``locked``, ``online``, ``door_position_status``,\n``lock_status``, and ``last_activity`` (epoch ms).\n\nExample: list_doors(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_doors"
    },
    {
      "description": "List Dynamic DNS update configurations on the controller.\n\nSide effects: None (read-only).\n\nReturns one record per DDNS config with ``_id``, ``service``\n(provider), ``host_name`` (the FQDN kept updated), ``login``,\n``server`` (custom update-URL host, if any), ``interface`` (tracked\nWAN), and ``enabled``. The provider password (``x_password``) is\nwrite-only and comes back redacted. Empty on a gateway with no DDNS\nconfigured.\n\nExample: list_dynamic_dns(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_dynamic_dns"
    },
    {
      "description": "List recent controller events (connections, disconnections, etc.).\n\nSide effects: None (read-only).\n\nReturns the most recent ``limit`` event records, newest first.\n\nExample: list_events(limit=100)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "limit": {
            "default": 50,
            "description": "Max number of events to return (default 50, max 1000).",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "list_events"
    },
    {
      "description": "Return only ``result == \"denied\"`` events within the window.\n\nSide effects: None (read-only).\n\nConvenience wrapper around ``list_access_events`` for security audits.\nEach entry includes ``reason`` (e.g. ``\"expired\"``, ``\"off-hours\"``,\n``\"unknown_pin\"``) so the LLM can summarise the cause distribution.\n\nExample: list_failed_access_attempts(hours_back=12, door_id=\"abc...\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "door_id": {
            "default": "",
            "description": "Optional door ``id`` filter. Empty returns denials\nacross every door.",
            "type": "string"
          },
          "hours_back": {
            "default": 24,
            "description": "Look-back window in hours. Defaults to 24.",
            "type": "integer"
          },
          "limit": {
            "default": 100,
            "description": "Maximum events returned. Defaults to 100.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "list_failed_access_attempts"
    },
    {
      "description": "List reusable firewall groups (address, IPv6-address, and port groups).\n\nSide effects: None (read-only).\n\nFirewall groups are named, reusable sets of addresses or ports that\nfirewall rules and other policies reference by ``_id`` instead of\nrepeating literals. Returns one record per group with ``_id``,\n``name``, ``group_type`` (``address-group``/``ipv6-address-group``/\n``port-group``), and ``group_members`` (the list of CIDRs, IPs, or\nports).\n\nExample: list_firewall_groups(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_firewall_groups"
    },
    {
      "description": "List every firewall rule on the controller.\n\nSide effects: None (read-only).\n\nReturns one record per rule with ``_id``, ``name``, ``ruleset``,\n``rule_index``, ``action``, ``enabled``, ``protocol``, and\n``src_*``/``dst_*`` fields.\n\nExample: list_firewall_rules(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_firewall_rules"
    },
    {
      "description": "List configured honeypots and the global honeypot toggle.\n\nSide effects: None (read-only).\n\nReturns ``{\"enabled\": bool, \"honeypots\": [...]}`` where each entry\ncarries ``id`` (synthesised ``network_id:ip``), ``network_id``,\n``network_name`` (looked up from ``list_networks``), ``ip``, and\n``version`` (always ``v4`` on UniFi 9.x).\n\nExample: list_honeypots(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_honeypots"
    },
    {
      "description": "List motion events in the last ``hours_back`` hours.\n\nSide effects: None (read-only).\n\nFilters Protect's event stream to entries with ``type == \"motion\"``\nwithin the requested window. ``camera_id`` narrows the result set to\none camera; an empty string returns motion across every bonded camera.\n\nExample: list_motion_events(camera_id=\"65f...\", limit=20, hours_back=6)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "default": "",
            "description": "Protect camera ``id``. Empty (default) returns motion\nacross all cameras.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "hours_back": {
            "default": 24,
            "description": "Look-back window in hours from now. Defaults to 24.",
            "type": "integer"
          },
          "limit": {
            "default": 50,
            "description": "Maximum number of events returned. Defaults to 50.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "list_motion_events"
    },
    {
      "description": "List every network/VLAN configured on the controller.\n\nSide effects: None (read-only).\n\nReturns one record per network with ``_id``, ``name``, ``purpose``,\n``vlan``, ``vlan_enabled``, ``ip_subnet``, ``dhcpd_enabled``,\n``dhcpd_start``, ``dhcpd_stop``, and ``enabled``. Records also carry\ntheir IPv6 state inline: ``ipv6_interface_type`` (``none``/``pd``/\n``static``), ``ipv6_ra_enabled`` (router advertisements), and\n``ipv6_client_address_assignment`` (``slaac``/``dhcpv6``). Use\n``set_lan_ipv6`` to change those, or ``get_wan_ipv6`` /\n``set_wan_ipv6`` for the WAN uplink's IPv6 config.\n\nExample: list_networks(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_networks"
    },
    {
      "description": "List every port-forward (DNAT) rule on the controller.\n\nSide effects: None (read-only).\n\nReturns one record per forward with ``_id``, ``name``, ``enabled``,\n``proto``, ``src``, ``fwd``, ``fwd_port``, and ``dst_port``.\n\nExample: list_port_forwards(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_port_forwards"
    },
    {
      "description": "List switch port profiles configured on the controller.\n\nSide effects: None (read-only).\n\nPort profiles control PoE, native VLAN, tagged VLANs, and STP per\nswitch port. Returns one record per profile with ``_id``, ``name``,\n``native_networkconf_id``, ``forward``, and ``poe_mode``. Use these\nIDs when assigning ports on a switch.\n\nExample: list_port_profiles(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_port_profiles"
    },
    {
      "description": "List Protect recordings for one camera over the last ``hours_back`` hours.\n\nSide effects: None (read-only).\n\nReturns one record per stored clip segment with ``id``, ``start``,\n``end``, ``type``, and ``size`` (bytes). Useful for finding the\nrecording window around a specific motion event.\n\nExample: list_recordings(camera_id=\"65f...\", hours_back=6)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "description": "Protect camera ``id``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "hours_back": {
            "default": 24,
            "description": "Look-back window in hours. Defaults to 24.",
            "type": "integer"
          }
        },
        "required": [
          "camera_id"
        ],
        "type": "object"
      },
      "name": "list_recordings"
    },
    {
      "description": "List user-defined static (next-hop) routes on the controller.\n\nSide effects: None (read-only).\n\nReturns one record per route with ``_id``, ``name``, ``type``,\n``static-route_network`` (destination CIDR),\n``static-route_nexthop`` (next-hop IP), ``static-route_distance``,\nand ``enabled``. Empty on a gateway with no custom routes (the default\nroute is managed by the WAN config, not listed here).\n\nExample: list_routes(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_routes"
    },
    {
      "description": "List smart-detection events (person, vehicle, animal, package).\n\nSide effects: None (read-only).\n\nFilters Protect's event stream to ``type == \"smartDetectZone\"`` and\nnarrows to entries whose ``smartDetectTypes`` contain\n``detection_type``. Empty ``camera_id`` returns detections across all\ncameras.\n\nExample: list_smart_detections(detection_type=\"person\", hours_back=12)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "default": "",
            "description": "Protect camera ``id``. Empty (default) returns matches\nacross all cameras.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "detection_type": {
            "default": "person",
            "description": "One of ``\"person\"``, ``\"vehicle\"``, ``\"animal\"``,\n``\"package\"``. Defaults to ``\"person\"``.",
            "type": "string"
          },
          "hours_back": {
            "default": 24,
            "description": "Look-back window in hours. Defaults to 24.",
            "type": "integer"
          },
          "limit": {
            "default": 20,
            "description": "Maximum number of events returned. Defaults to 20.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "list_smart_detections"
    },
    {
      "description": "List top clients by total bytes (DPI by-station report).\n\nSide effects: None (read-only).\n\nReturns one record per client ranked by ``total_bytes`` descending,\nwith ``mac``, ``hostname``, ``ip``, ``tx_bytes``, ``rx_bytes``, and\n``total_bytes``.\n\nExample: list_top_talkers(limit=10)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "limit": {
            "default": 10,
            "description": "Max number of clients to return (default 10, max 1000).",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "list_top_talkers"
    },
    {
      "description": "List v2 traffic routes (policy-based routing, e.g. VPN-client routes).\n\nSide effects: None (read-only).\n\nReturns one record per route with ``_id``, ``matching_target``,\n``next_hop`` / ``network_id`` (the interface or VPN the matched\ntraffic is sent out), ``kill_switch_enabled``, and ``enabled``. Empty\non a gateway with no policy routes configured.\n\nExample: list_traffic_routes(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_traffic_routes"
    },
    {
      "description": "List v2 traffic rules (app/domain/IP-based allow & block policies).\n\nSide effects: None (read-only).\n\nReturns one record per rule with ``_id``, ``action``\n(``BLOCK``/``ALLOW``), ``matching_target``, ``target_devices``, and\n``enabled``. Empty on a gateway with no traffic rules configured.\n\nExample: list_traffic_rules(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_traffic_rules"
    },
    {
      "description": "List every visitor pass on the controller (active and expired).\n\nSide effects: None (read-only).\n\nReturns one record per visitor with ``id``, ``first_name``,\n``last_name``, ``full_name``, ``email``, ``host_user_id``,\n``valid_from`` (ms), ``valid_until`` (ms), ``status``, and\n``pass_code``.\n\nExample: list_visitors(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_visitors"
    },
    {
      "description": "List every WiFi SSID configured on the controller.\n\nSide effects: None (read-only).\n\nReturns one record per WLAN with ``_id``, ``name``, ``enabled``,\n``security``, ``wpa_mode``, ``networkconf_id``, ``is_guest``,\n``hide_ssid``, and ``wlan_band``.\n\nExample: list_wlans(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "list_wlans"
    },
    {
      "description": "Toggle the LED locate beacon on a device.\n\nSide effects:\n- The physical device flashes its status LED until ``on=False`` is\n  sent (or it reboots). Helpful for finding which physical AP or\n  switch maps to a controller record.\n- No traffic impact.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: locate_device(mac=\"aa:bb:cc:00:00:01\", on=True)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "mac": {
            "description": "Device MAC address (from ``list_devices``).",
            "type": "string"
          },
          "on": {
            "default": true,
            "description": "``True`` (default) starts the flash; ``False`` stops it.",
            "type": "boolean"
          }
        },
        "required": [
          "mac"
        ],
        "type": "object"
      },
      "name": "locate_device"
    },
    {
      "description": "Configure a camera end-to-end: recording mode + sensitivity + privacy.\n\nSide effects:\n- Step 1: PATCHes ``recordingSettings.mode`` and\n  ``recordingSettings.retentionDurationMs`` (computed from\n  ``retention_days`` * 86400000).\n- Step 2: PATCHes ``motionSettings.sensitivity``.\n- Step 3: PATCHes ``privacyMask.enabled``.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n- Rollback: if step 2 or step 3 fails, the camera's original\n  recording, sensitivity, and privacy settings are restored from the\n  pre-call snapshot. The response includes ``rolled_back`` and\n  ``partial`` keys describing what was applied and what was\n  reverted.\n\nExample: provision_camera(camera_id=\"65f...\", recording_mode=\"motion\", sensitivity=60, retention_days=14)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "description": "Protect camera ``id``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "privacy_enabled": {
            "default": false,
            "description": "``True`` engages the privacy mask after the\nrecording / sensitivity steps. Defaults to ``False``.",
            "type": "boolean"
          },
          "recording_mode": {
            "default": "motion",
            "description": "One of ``\"always\"``, ``\"motion\"``, ``\"never\"``.\nDefaults to ``\"motion\"``.",
            "type": "string"
          },
          "retention_days": {
            "default": 7,
            "description": "Clip retention in days. Multiplied by 86,400,000\nto derive ``retentionDurationMs``. Defaults to 7.",
            "type": "integer"
          },
          "sensitivity": {
            "default": 50,
            "description": "Motion sensitivity 0-100. Defaults to 50.",
            "type": "integer"
          }
        },
        "required": [
          "camera_id"
        ],
        "type": "object"
      },
      "name": "provision_camera"
    },
    {
      "description": "Stand up a homelab service: DHCP lease + firewall allow + (optional) port forwards.\n\nSide effects:\n- Step 1: creates a static DHCP reservation pinning ``mac`` to\n  ``ip`` on the named network.\n- Step 2 (when ``ports`` is non-empty): creates a LAN_LOCAL accept\n  firewall rule for ``ip`` on the requested TCP ports.\n- Step 3 (when ``wan_expose=True`` and ``ports`` is non-empty):\n  creates one port-forward rule per port, exposing the service to\n  the WAN.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n- Rollback: if any sub-step fails, all prior sub-steps are reverted\n  in reverse order (port_forwards \u2192 firewall_rule \u2192 lease) and the\n  response includes ``rolled_back`` and ``partial`` keys.\n\nExample: provision_homelab_service(name=\"plex\", mac=\"aa:bb:cc:00:00:01\", ip=\"10.50.0.10\", network_id=\"65f...\", ports=[32400], wan_expose=False)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "ip": {
            "description": "IPv4 address to reserve (must fall inside the network's\nsubnet).",
            "type": "string"
          },
          "mac": {
            "description": "Client MAC address (e.g. ``\"aa:bb:cc:00:00:01\"``).",
            "type": "string"
          },
          "name": {
            "description": "Display name (used for both the lease and the firewall\nrule, e.g. ``\"plex\"``).",
            "type": "string"
          },
          "network_id": {
            "description": "``_id`` of the network/VLAN this service lives on.",
            "type": "string"
          },
          "ports": {
            "anyOf": [
              {
                "items": {
                  "type": "integer"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "TCP ports the service listens on (e.g. ``[80, 443]``).\nEmpty creates only the lease."
          },
          "wan_expose": {
            "default": false,
            "description": "``True`` also creates port-forward rules so the\nservice is reachable from the WAN. Default ``False``\n(LAN-only).",
            "type": "boolean"
          }
        },
        "required": [
          "name",
          "mac",
          "ip",
          "network_id"
        ],
        "type": "object"
      },
      "name": "provision_homelab_service"
    },
    {
      "description": "Block a client and log the quarantine action with a reason.\n\nSide effects:\n- Step 1: adds the MAC to the controller's user-block list (same as\n  ``block_client``). The client is immediately disconnected and\n  prevented from re-associating until ``unblock_client`` is called.\n- Step 2: appends a WARNING-level structured log entry capturing\n  ``mac`` and ``reason`` for later forensics.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n- Rollback: if any sub-step fails, all prior sub-steps are reverted\n  (the log entry is best-effort and not unwound; only step 1\n  mutates the controller).\n\nExample: quarantine_client(mac=\"aa:bb:cc:00:00:01\", reason=\"suspected malware c2\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "mac": {
            "description": "Client MAC address (e.g. ``\"aa:bb:cc:00:00:01\"``).",
            "type": "string"
          },
          "reason": {
            "default": "",
            "description": "Free-form justification (kept in logs only,\ne.g. ``\"suspected malware c2\"``).",
            "type": "string"
          }
        },
        "required": [
          "mac"
        ],
        "type": "object"
      },
      "name": "quarantine_client"
    },
    {
      "description": "Force a client to reconnect (kick-sta).\n\nSide effects:\n- Issues a deauthentication frame for ``mac`` on its current AP. The\n  client immediately disconnects and most clients re-associate\n  automatically within seconds.\n- The block-list is not modified; this is a transient nudge.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: reconnect_client(mac=\"aa:bb:cc:00:00:01\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "mac": {
            "description": "Client MAC address (e.g. ``\"aa:bb:cc:00:00:01\"``).",
            "type": "string"
          }
        },
        "required": [
          "mac"
        ],
        "type": "object"
      },
      "name": "reconnect_client"
    },
    {
      "description": "Rename an adopted UniFi device (AP, switch, or gateway).\n\nSide effects:\n- Cosmetic only: changes the display name in the controller and in\n  tool output. No RF or traffic impact, no client disconnects.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: rename_device(device_mac=\"aa:bb:cc:00:00:02\", name=\"Bedroom AP\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "device_mac": {
            "description": "Device MAC address (from ``list_devices``).",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "name": {
            "description": "New display name. Must be non-empty.",
            "type": "string"
          }
        },
        "required": [
          "device_mac",
          "name"
        ],
        "type": "object"
      },
      "name": "rename_device"
    },
    {
      "description": "Restart an adopted UniFi device (gateway, AP, or switch).\n\nSide effects:\n- Issues a soft reboot. The device is unreachable for ~60 seconds.\n- Wireless clients on a restarted AP will be disconnected and must\n  re-associate.\n- Restarting a gateway interrupts WAN/LAN traffic for the duration\n  of the reboot.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: restart_device(mac=\"aa:bb:cc:00:00:01\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "mac": {
            "description": "Device MAC address (from ``list_devices``).",
            "type": "string"
          }
        },
        "required": [
          "mac"
        ],
        "type": "object"
      },
      "name": "restart_device"
    },
    {
      "description": "Restore controller state from a ``backup_config`` envelope.\n\nSide effects:\n- Diffs the envelope against current controller state and applies\n  an ordered create/delete plan. Deletes run first (firewall_rules,\n  port_forwards, wlans, dhcp_leases, port_profiles, networks);\n  creates run second in the reverse order. No update path in this\n  version: drifted fields show up as a delete + create pair (or\n  stay put when names match).\n- Cross-controller restore: if the envelope's ``controller``\n  differs from the target, the restore proceeds and the response\n  includes a warning.\n- Stripped secrets: if ``secrets_stripped: true``, every restored\n  WLAN whose ``x_passphrase`` still equals the sentinel is forced\n  to ``enabled=False`` so a known-string SSID is never broadcast.\n  The operator must reset each passphrase and re-enable manually.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n- Rollback: if any sub-step fails, all prior creates are reverted\n  in reverse order and the response includes ``partial`` and\n  ``rolled_back`` keys. Deletes that already ran are NOT undone\n  (no copy was kept); the response reports them so the operator\n  can re-run with ``dry_run`` to inspect the residual gap.\n\nSchema validation: ``schema != \"1\"`` returns an error envelope and\nno action is taken.\n\nExample: restore_config(backup_json=snapshot, dry_run=True)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "backup_json": {
            "description": "The envelope JSON string from ``backup_config``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          }
        },
        "required": [
          "backup_json"
        ],
        "type": "object"
      },
      "name": "restore_config"
    },
    {
      "description": "Toggle a camera's privacy mask (lens cover) on or off.\n\nSide effects:\n- PATCHes ``privacyMask.enabled`` on the camera. When ``True``, the\n  camera stops capturing video; clips are not recorded.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: set_camera_privacy_mode(camera_id=\"65f...\", enabled=True)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "description": "Protect camera ``id``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "enabled": {
            "description": "``True`` to engage the privacy mask, ``False`` to release\nit.",
            "type": "boolean"
          }
        },
        "required": [
          "camera_id",
          "enabled"
        ],
        "type": "object"
      },
      "name": "set_camera_privacy_mode"
    },
    {
      "description": "Set a camera's recording mode (``always``, ``motion``, or ``never``).\n\nSide effects:\n- PATCHes ``recordingSettings.mode`` on the camera. Takes effect\n  immediately; ``never`` halts new clip writes.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: set_camera_recording_mode(camera_id=\"65f...\", mode=\"motion\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "description": "Protect camera ``id``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "mode": {
            "description": "One of ``\"always\"``, ``\"motion\"``, ``\"never\"``.",
            "type": "string"
          }
        },
        "required": [
          "camera_id",
          "mode"
        ],
        "type": "object"
      },
      "name": "set_camera_recording_mode"
    },
    {
      "description": "Set the IPv6 configuration of one LAN/VLAN network.\n\nSide effects:\n- Changes how this network gets and advertises IPv6. Turning on\n  ``interface_type=\"pd\"`` plus ``ra_enabled=True`` is what makes the\n  LAN's clients receive global IPv6 addresses from the prefix\n  delegated by the WAN. Toggling router advertisements\n  (``ra_enabled``) or the address-assignment mode causes clients on\n  this network to re-acquire IPv6; IPv4 is unaffected.\n- Only the IPv6 keys you supply change; every other field on the\n  network record is read first and written back unchanged (strict\n  read-modify-write).\n- Mutates controller state. Use dry_run=True to preview the before/\n  after diff without applying.\n\nRead first: call ``list_networks`` to find the ``network_id`` and see\nthe current ``ipv6_*`` state (now surfaced inline).\n\nExample: set_lan_ipv6(network_id=\"65f...\", interface_type=\"pd\", ra_enabled=True, dry_run=True)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "address_assignment": {
            "default": "",
            "description": "``\"slaac\"`` or ``\"dhcpv6\"``. Empty leaves it\nunchanged.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dns_auto": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "``True`` to advertise the gateway as DNS, ``False`` to\nuse explicit servers from ``dns_servers``. ``None`` leaves it\nunchanged."
          },
          "dns_servers": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Up to four IPv6 DNS server addresses, applied as\n``dhcpdv6_dns_1..4`` when ``dns_auto=False``. ``None`` leaves\nthem unchanged."
          },
          "dry_run": {
            "default": false,
            "description": "Preview the before/after diff without applying it.",
            "type": "boolean"
          },
          "interface_type": {
            "default": "",
            "description": "``\"none\"``, ``\"pd\"`` (use the WAN-delegated\nprefix), or ``\"static\"``. Empty leaves it unchanged.",
            "type": "string"
          },
          "network_id": {
            "description": "The ``_id`` from ``list_networks`` (a LAN/VLAN, not a\nWAN).",
            "type": "string"
          },
          "ra_enabled": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "``True``/``False`` to toggle IPv6 Router\nAdvertisements (SLAAC). ``None`` (default) leaves it unchanged."
          }
        },
        "required": [
          "network_id"
        ],
        "type": "object"
      },
      "name": "set_lan_ipv6"
    },
    {
      "description": "Set a camera's motion sensitivity (0-100).\n\nSide effects:\n- PATCHes ``motionSettings.sensitivity``. Higher values trigger more\n  motion events; 0 effectively disables motion-based recording even\n  when ``recordingSettings.mode`` is ``\"motion\"``.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: set_motion_sensitivity(camera_id=\"65f...\", sensitivity=60)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "camera_id": {
            "description": "Protect camera ``id``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "sensitivity": {
            "description": "Integer 0-100. Out-of-range values are rejected.",
            "type": "integer"
          }
        },
        "required": [
          "camera_id",
          "sensitivity"
        ],
        "type": "object"
      },
      "name": "set_motion_sensitivity"
    },
    {
      "description": "Override settings on a single switch port.\n\nSide effects:\n- Modifies one port's ``enable``, ``poe_mode``, and/or\n  ``portconf_id`` on the named switch without touching the others.\n- Disabling a port immediately drops the link; powered devices on\n  that port (PoE cameras, APs) will go offline.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: set_port_state(device_mac=\"aa:bb:cc:00:00:01\", port_idx=5, enable=True, poe_mode=\"auto\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "device_mac": {
            "description": "Switch MAC address (from ``list_devices``).",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "enable": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "``True`` to bring the port up, ``False`` to disable.\n``None`` (default) leaves it unchanged."
          },
          "poe_mode": {
            "default": "",
            "description": "``\"auto\"``, ``\"passive24v\"``, ``\"passthrough\"``,\n``\"off\"``, or empty to leave unchanged.",
            "type": "string"
          },
          "port_idx": {
            "description": "1-based port index.",
            "type": "integer"
          },
          "portconf_id": {
            "default": "",
            "description": "``_id`` of a port profile to apply, or empty to\nleave unchanged.",
            "type": "string"
          }
        },
        "required": [
          "device_mac",
          "port_idx"
        ],
        "type": "object"
      },
      "name": "set_port_state"
    },
    {
      "description": "Set the channel and/or channel width of one radio on an AP.\n\nSide effects:\n- The radio retunes immediately; wireless clients on that band\n  briefly disconnect and re-associate.\n- On 5GHz, DFS channels (52-144) may add a radar-scan delay before\n  the radio starts serving clients.\n- Only the targeted radio changes; everything else on the device is\n  read first and written back unchanged.\n- Mutates controller state. Use dry_run=True to preview the change\n  (including the current value) without applying.\n\nRead first: call ``get_device_radios`` to see the current channel\nand width.\n\nExample: set_radio_channel(device_mac=\"aa:bb:cc:00:00:02\", band=\"5g\", channel=\"36\", width_mhz=80)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "band": {
            "description": "``\"2g\"``, ``\"5g\"``, or ``\"6g\"`` (raw UniFi ids ``ng``,\n``na``, ``6e`` also accepted).",
            "type": "string"
          },
          "channel": {
            "default": "",
            "description": "``\"auto\"`` or a channel number as a string (e.g.\n``\"36\"``). Empty (default) leaves the channel unchanged.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "device_mac": {
            "description": "AP MAC address (from ``list_devices``).",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "width_mhz": {
            "default": 0,
            "description": "Channel width: 20, 40, 80, 160, 240, or 320.\n``0`` (default) leaves the width unchanged.",
            "type": "integer"
          }
        },
        "required": [
          "device_mac",
          "band"
        ],
        "type": "object"
      },
      "name": "set_radio_channel"
    },
    {
      "description": "Enable, tune, or disable minimum RSSI on one radio of an AP.\n\nSide effects:\n- With min-RSSI enabled, the AP kicks clients whose signal drops\n  below the threshold, forcing them to roam to a closer AP. A\n  threshold set too aggressive (e.g. -60) causes disconnect loops\n  for legitimately distant clients.\n- Only the targeted radio changes; everything else on the device is\n  read first and written back unchanged.\n- Mutates controller state. Use dry_run=True to preview the change\n  (including the current value) without applying.\n\nRead first: call ``get_device_radios`` to see the current state.\nTypical sticky-client thresholds are -75 to -70 dBm.\n\nExample: set_radio_min_rssi(device_mac=\"aa:bb:cc:00:00:02\", band=\"5g\", enabled=True, rssi_dbm=-75)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "band": {
            "description": "``\"2g\"``, ``\"5g\"``, or ``\"6g\"`` (raw UniFi ids ``ng``,\n``na``, ``6e`` also accepted).",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "device_mac": {
            "description": "AP MAC address (from ``list_devices``).",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "enabled": {
            "description": "``True`` enforces the threshold; ``False`` turns\nmin-RSSI off for this radio.",
            "type": "boolean"
          },
          "rssi_dbm": {
            "default": 0,
            "description": "Threshold in dBm (negative, e.g. ``-75``). Required\nwhen ``enabled=True``.",
            "type": "integer"
          }
        },
        "required": [
          "device_mac",
          "band",
          "enabled"
        ],
        "type": "object"
      },
      "name": "set_radio_min_rssi"
    },
    {
      "description": "Set the transmit power mode of one radio on an access point.\n\nSide effects:\n- Changes the AP's RF footprint. Lowering TX power shrinks coverage;\n  clients at the cell edge may roam to another AP or drop to a lower\n  PHY rate. The radio applies the change within seconds (brief\n  client re-association possible).\n- Only the targeted radio changes; the device's other radios and\n  settings are read first and written back unchanged.\n- Mutates controller state. Use dry_run=True to preview the change\n  (including the current value) without applying.\n\nRead first: call ``get_device_radios`` to see the current mode and\nthe radio's supported dBm range before changing it.\n\nExample: set_radio_tx_power(device_mac=\"aa:bb:cc:00:00:02\", band=\"5g\", mode=\"medium\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "band": {
            "description": "``\"2g\"``, ``\"5g\"``, or ``\"6g\"`` (raw UniFi ids ``ng``,\n``na``, ``6e`` also accepted).",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "device_mac": {
            "description": "AP MAC address (from ``list_devices``).",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "mode": {
            "description": "``\"auto\"``, ``\"high\"``, ``\"medium\"``, ``\"low\"``, or\n``\"custom\"`` (requires ``power_dbm``).",
            "type": "string"
          },
          "power_dbm": {
            "default": 0,
            "description": "Exact transmit power in dBm, only used with\n``mode=\"custom\"``. Validated against the radio's\n``min_txpower``/``max_txpower`` bounds.",
            "type": "integer"
          }
        },
        "required": [
          "device_mac",
          "band",
          "mode"
        ],
        "type": "object"
      },
      "name": "set_radio_tx_power"
    },
    {
      "description": "Toggle the Teleport VPN listener.\n\nSide effects:\n- Patches the ``teleport`` setting record's ``enabled`` field.\n  When ``False``, the controller tears down the listener and\n  disconnects any active sessions. Registered clients (via the\n  mobile app) remain registered and reconnect once the listener\n  is re-enabled.\n- Mutates controller state. Use dry_run=True to preview.\n\nExample: set_teleport_enabled(enabled=True)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it.",
            "type": "boolean"
          },
          "enabled": {
            "description": "``True`` activates the listener; ``False`` disables it.",
            "type": "boolean"
          }
        },
        "required": [
          "enabled"
        ],
        "type": "object"
      },
      "name": "set_teleport_enabled"
    },
    {
      "description": "Update Threat Management (IDS/IPS) configuration.\n\nSide effects:\n- Patches the controller's ``ips`` setting record. When\n  ``enabled=False`` the mode is forced to ``off``; the controller\n  then disables packet inspection on every network. When\n  ``enabled=True``, ``mode`` selects detection-only (``ids``) or\n  inline blocking (``ips``).\n- Untouched fields (signature categories, network bindings, ad\n  blocking, DNS filtering) are preserved by the controller's merge\n  semantics. Supplying ``signature_categories`` replaces the active\n  list wholesale; ``None`` leaves it unchanged.\n- Mutates controller state. Use dry_run=True to preview the change.\n\nExample: set_threat_management(enabled=True, mode=\"ips\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted patch envelope.",
            "type": "boolean"
          },
          "enabled": {
            "description": "``True`` activates inspection; ``False`` sets ``mode=off``\nand overrides any ``mode`` argument.",
            "type": "boolean"
          },
          "mode": {
            "default": "ids",
            "description": "When ``enabled=True``, one of ``\"ids\"`` (detection-only,\ndefault) or ``\"ips\"`` (inline blocking). Ignored when\n``enabled=False``.",
            "type": "string"
          },
          "signature_categories": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Optional replacement list of UniFi\nsignature-category strings (e.g. ``[\"emerging-malware\",\n\"tor\", \"dshield\"]``). Pass ``None`` to leave the active\ncategories untouched."
          }
        },
        "required": [
          "enabled"
        ],
        "type": "object"
      },
      "name": "set_threat_management"
    },
    {
      "description": "Set the WAN uplink's IPv6 connection type and prefix delegation.\n\nSide effects:\n- Changes how the gateway obtains IPv6 on the WAN. **Changing\n  ``connection_type`` tears down and re-establishes the WAN IPv6\n  session**: existing IPv6 connections drop and the gateway\n  re-negotiates with the ISP (DHCPv6/SLAAC). IPv4 connectivity is not\n  affected, but IPv6 hosts lose reachability for a few seconds to a\n  minute. Enabling delegation (``prefix-delegation``) is what hands\n  IPv6 prefixes down to your LANs.\n- Only the IPv6 keys you supply change; every other field on the WAN\n  record is read first and written back unchanged (strict\n  read-modify-write).\n- Mutates controller state. Use dry_run=True to preview the before/\n  after diff without applying.\n\nRead first: call ``get_wan_ipv6`` to see the current values.\n\nBLAST RADIUS: this is a WAN-level change. On dual-WAN gateways it only\ntouches the named WAN. Do not run this against the live gateway without\nintent: a mistyped ``connection_type`` can leave the WAN with no IPv6.\n\nExample: set_wan_ipv6(connection_type=\"dhcpv6\", prefix_delegation=\"prefix-delegation\", dry_run=True)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "connection_type": {
            "default": "",
            "description": "``\"disabled\"``, ``\"dhcpv6\"``, ``\"pppoe\"``, or\n``\"static\"``. Empty leaves it unchanged. Most ISPs that hand\nout IPv6 (incl. Empire Access) use ``\"dhcpv6\"``.",
            "type": "string"
          },
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dns_preference": {
            "default": "",
            "description": "``\"auto\"`` or ``\"manual\"`` for IPv6 DNS. Empty\nleaves it unchanged.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the before/after diff without applying it.",
            "type": "boolean"
          },
          "pd_size": {
            "default": 0,
            "description": "DHCPv6-PD prefix size (48-64), e.g. ``56``. Only sent when\nnon-zero; sending it also pins ``wan_dhcpv6_pd_size_auto`` to\n``false``. Leave 0 to keep the controller's auto sizing.",
            "type": "integer"
          },
          "prefix_delegation": {
            "default": "",
            "description": "``\"none\"`` or ``\"prefix-delegation\"``. Empty\nleaves it unchanged. ``\"prefix-delegation\"`` is required for\nyour LANs to receive IPv6 subnets.",
            "type": "string"
          },
          "wan_name": {
            "default": "",
            "description": "Display name of the WAN to target (e.g. ``\"Internet 1\"``).\nRequired only on multi-WAN gateways; single-WAN gateways match\nautomatically.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "set_wan_ipv6"
    },
    {
      "description": "Aggregate badge events by door, user, and outcome.\n\nSide effects: None (read-only).\n\nPulls events over the last ``hours_back`` hours (cap 500 to keep the\nsummary tractable) and rolls them up into counts so the LLM can\nanswer \"who used which door how often\" without re-aggregating\nclient-side. Returns ``total``, ``granted``, ``denied``, plus\n``by_door`` and ``by_user`` maps.\n\nExample: summarize_access_activity(hours_back=24)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target.",
            "type": "string"
          },
          "hours_back": {
            "default": 24,
            "description": "Look-back window in hours. Defaults to 24.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "name": "summarize_access_activity"
    },
    {
      "description": "Enable or disable a traffic route without editing its other fields.\n\nSide effects:\n- Flips the route's ``enabled`` flag. Reads the current route first\n  and PUTs the full object back with only ``enabled`` changed\n  (read-modify-write), so the rest of the route is preserved.\n- Takes effect immediately: a disabled route stops steering its\n  matched traffic, which then follows the default route.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: toggle_traffic_route(route_id=\"65f...\", enabled=False)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "enabled": {
            "description": "``True`` to enable the route, ``False`` to disable it.",
            "type": "boolean"
          },
          "route_id": {
            "description": "The ``_id`` from ``list_traffic_routes``.",
            "type": "string"
          }
        },
        "required": [
          "route_id",
          "enabled"
        ],
        "type": "object"
      },
      "name": "toggle_traffic_route"
    },
    {
      "description": "Enable or disable a traffic rule without editing its other fields.\n\nSide effects:\n- Flips the rule's ``enabled`` flag. Reads the current rule first and\n  PUTs the full object back with only ``enabled`` changed\n  (read-modify-write), so the rest of the rule is preserved.\n- Takes effect immediately: a disabled BLOCK rule stops blocking; a\n  disabled ALLOW rule stops allowing.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: toggle_traffic_rule(rule_id=\"65f...\", enabled=False)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "enabled": {
            "description": "``True`` to enable the rule, ``False`` to disable it.",
            "type": "boolean"
          },
          "rule_id": {
            "description": "The ``_id`` from ``list_traffic_rules``.",
            "type": "string"
          }
        },
        "required": [
          "rule_id",
          "enabled"
        ],
        "type": "object"
      },
      "name": "toggle_traffic_rule"
    },
    {
      "description": "Kick off a UniFi speed test on the WAN link.\n\nSide effects: None on the controller's persistent state. Issues a\none-shot WAN measurement that consumes WAN bandwidth for ~30-60\nseconds while the test runs server-side. Use\n``get_speedtest_results`` to read the result once it finishes.\n\nExample: trigger_speedtest(controller=\"default\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "name": "trigger_speedtest"
    },
    {
      "description": "Unblock a previously-blocked client by MAC.\n\nSide effects:\n- Removes the MAC from the controller's user-block list. The client\n  can re-associate immediately.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: unblock_client(mac=\"aa:bb:cc:00:00:01\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "mac": {
            "description": "Client MAC address (e.g. ``\"aa:bb:cc:00:00:01\"``).",
            "type": "string"
          }
        },
        "required": [
          "mac"
        ],
        "type": "object"
      },
      "name": "unblock_client"
    },
    {
      "description": "Patch fields on an existing content-filtering profile (read-modify-write).\n\nSide effects:\n- Modifies the named profile in place. The v2 PUT replaces the whole\n  object, so this reads the current profile first and merges\n  ``updates`` onto it before writing: only the keys you supply change,\n  everything else (scope, schedule, the other lists) is preserved.\n- Takes effect immediately on DNS resolution for the in-scope clients\n  and networks: enabling a category starts blocking it; adding a\n  ``block_list`` domain starts NXDOMAIN-ing it.\n- List fields (``categories``, ``allow_list``, ``block_list``,\n  ``client_macs``, ``network_ids``, ``safe_search``) are **replaced\n  wholesale**, not appended, so pass the full desired list. Read the\n  current value with ``get_content_filter_details`` first.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_content_filter(filter_id=\"65f...\", updates={\"enabled\": False})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "filter_id": {
            "description": "The ``_id`` from ``list_content_filters``.",
            "type": "string"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial profile record to merge. Common keys: ``name``,\n``enabled``, ``categories`` (full blocked-category list),\n``allow_list`` / ``block_list`` (full per-domain lists),\n``client_macs`` / ``network_ids`` (full scope lists),\n``safe_search``, ``schedule``.",
            "type": "object"
          }
        },
        "required": [
          "filter_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_content_filter"
    },
    {
      "description": "Patch fields on an existing Dynamic DNS config.\n\nSide effects:\n- Modifies the named DDNS config in place. Only fields supplied in\n  ``updates`` change; everything else is preserved.\n- Takes effect immediately: changing ``host_name`` or ``service``\n  repoints the next update; toggling ``enabled`` starts or stops\n  updates.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_dynamic_dns(ddns_id=\"65f...\", updates={\"enabled\": False})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "ddns_id": {
            "description": "The ``_id`` from ``list_dynamic_dns``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set (``x_password`` redacted if present).",
            "type": "boolean"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial DDNS record. Common keys: ``service``,\n``host_name``, ``login``, ``x_password`` (new password/token),\n``server``, ``interface``, ``enabled``.",
            "type": "object"
          }
        },
        "required": [
          "ddns_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_dynamic_dns"
    },
    {
      "description": "Rename a firewall group or replace its members (read-modify-write).\n\nSide effects:\n- Replaces the group's name and/or members in place. The members list\n  is **replaced wholesale**, not merged, so pass the full desired set.\n  The group's ``group_type`` is preserved (it is read first and\n  written back unchanged); group type cannot be changed after\n  creation, only members.\n- Every rule referencing this group immediately sees the new members.\n- Mutates controller state. Use dry_run=True to preview the before/\n  after diff without applying.\n\nRead first: call ``get_firewall_group_details`` to see the current\nmembers.\n\nExample: update_firewall_group(group_id=\"65f...\", members=[\"10.50.0.0/24\", \"10.70.0.0/24\"])",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the before/after diff without applying it.",
            "type": "boolean"
          },
          "group_id": {
            "description": "The ``_id`` from ``list_firewall_groups``.",
            "type": "string"
          },
          "members": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "New full member list (replaces the old one). ``None``\n(default) keeps the current members."
          },
          "name": {
            "default": "",
            "description": "New display name. Empty (default) keeps the current name.",
            "type": "string"
          }
        },
        "required": [
          "group_id"
        ],
        "type": "object"
      },
      "name": "update_firewall_group"
    },
    {
      "description": "Patch fields on an existing firewall rule.\n\nSide effects:\n- Modifies the named rule in place. Only fields supplied in\n  ``updates`` change; everything else is preserved.\n- Takes effect immediately on the next packet hitting the affected\n  datapath.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_firewall_rule(rule_id=\"65f...\", updates={\"enabled\": False})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "rule_id": {
            "description": "The ``_id`` from ``list_firewall_rules``.",
            "type": "string"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial firewall-rule record. Common keys: ``enabled``,\n``action``, ``protocol``, ``rule_index``, ``src_address``,\n``dst_address``, ``src_networkconf_id``,\n``dst_networkconf_id``, ``src_port``, ``dst_port``, ``name``.",
            "type": "object"
          }
        },
        "required": [
          "rule_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_firewall_rule"
    },
    {
      "description": "Patch fields on an existing port-forward rule.\n\nSide effects:\n- Modifies the named forward in place. Only fields supplied in\n  ``updates`` change.\n- Takes effect immediately on the next inbound packet.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_port_forward(forward_id=\"65f...\", updates={\"enabled\": False})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "forward_id": {
            "description": "The ``_id`` from ``list_port_forwards``.",
            "type": "string"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial record. Common keys: ``enabled``, ``fwd_port``,\n``dst_port``, ``proto``, ``src``, ``fwd``, ``name``.",
            "type": "object"
          }
        },
        "required": [
          "forward_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_port_forward"
    },
    {
      "description": "Patch fields on an existing port profile.\n\nSide effects:\n- Modifies the named profile in place. Every switch port currently\n  using the profile picks up the new behaviour on the next\n  provision (typically within seconds).\n- Changing ``native_networkconf_id`` may move powered devices to a\n  different VLAN.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_port_profile(profile_id=\"65f...\", updates={\"poe_mode\": \"off\"})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "profile_id": {
            "description": "The ``_id`` from ``list_port_profiles``.",
            "type": "string"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial profile record. Common keys: ``name``,\n``forward``, ``poe_mode``, ``native_networkconf_id``,\n``tagged_networkconf_ids``.",
            "type": "object"
          }
        },
        "required": [
          "profile_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_port_profile"
    },
    {
      "description": "Patch fields on an existing static route.\n\nSide effects:\n- Modifies the named route in place. Only fields supplied in\n  ``updates`` change; everything else is preserved.\n- Takes effect immediately on the gateway's routing table.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_route(route_id=\"65f...\", updates={\"enabled\": False})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "route_id": {
            "description": "The ``_id`` from ``list_routes``.",
            "type": "string"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial route record. Common keys: ``name``, ``enabled``,\n``static-route_network`` (destination CIDR),\n``static-route_nexthop`` (next-hop IP),\n``static-route_distance``.",
            "type": "object"
          }
        },
        "required": [
          "route_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_route"
    },
    {
      "description": "Convert or update an existing client to a fixed-IP reservation.\n\nUse this instead of ``create_static_dhcp_lease`` when the MAC already\nhas a user record on the controller (any client that has ever\nconnected). The controller rejects POST ``/rest/user`` for known MACs\nwith ``api.err.MacUsed``; this tool resolves the existing ``_id`` and\nPUTs an update to ``/rest/user/{_id}`` instead.\n\nSide effects:\n- Sets ``use_fixedip=true`` and pins ``fixed_ip`` on the user record.\n- If ``local_dns_record`` is supplied, also sets\n  ``local_dns_record_enabled=true`` so the name resolves on the LAN\n  (controller version permitting).\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n- If no user record exists for ``mac``, the call fails with a clear\n  error. In that case use ``create_static_dhcp_lease`` instead.\n\nExample: update_static_dhcp_lease(mac=\"d0:11:e5:03:f6:3a\", fixed_ip=\"192.168.1.50\", network_id=\"6a0...\", name=\"cypher\")",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "fixed_ip": {
            "description": "IPv4 address to pin. Must be inside the network's subnet.",
            "type": "string"
          },
          "local_dns_record": {
            "default": "",
            "description": "Local DNS name to resolve to ``fixed_ip``\n(optional). When set, ``local_dns_record_enabled=true`` is\nalso sent.",
            "type": "string"
          },
          "mac": {
            "description": "Client MAC address (e.g. ``\"d0:11:e5:03:f6:3a\"``).",
            "type": "string"
          },
          "name": {
            "default": "",
            "description": "Friendly display name / hostname alias (optional).",
            "type": "string"
          },
          "network_id": {
            "description": "``_id`` of the network/VLAN the IP belongs to.",
            "type": "string"
          }
        },
        "required": [
          "mac",
          "fixed_ip",
          "network_id"
        ],
        "type": "object"
      },
      "name": "update_static_dhcp_lease"
    },
    {
      "description": "Patch fields on an existing traffic route (read-modify-write).\n\nSide effects:\n- Modifies the named policy route in place. The v2 PUT replaces the\n  whole object, so this reads the current route first and merges your\n  changes onto it before writing \u2014 only the keys you supply change.\n- Changes where matched traffic is routed (and, via\n  ``kill_switch_enabled``, whether it is dropped when the route's\n  interface is down). Takes effect immediately.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_traffic_route(route_id=\"65f...\", kill_switch_enabled=True)",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "kill_switch_enabled": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "When set, toggles the route's kill switch\n(drop matched traffic if the route's interface is down).\n``None`` (default) leaves it unchanged. Merged into ``updates``\nif both are given."
          },
          "route_id": {
            "description": "The ``_id`` from ``list_traffic_routes``.",
            "type": "string"
          },
          "updates": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Partial traffic-route record to merge. Common keys:\n``matching_target``, ``domains``, ``ip_addresses``,\n``next_hop``, ``network_id``, ``enabled``. ``None`` (default)\napplies no field changes beyond ``kill_switch_enabled``."
          }
        },
        "required": [
          "route_id"
        ],
        "type": "object"
      },
      "name": "update_traffic_route"
    },
    {
      "description": "Patch fields on an existing traffic rule (read-modify-write).\n\nSide effects:\n- Modifies the named rule in place. The v2 PUT replaces the whole\n  object, so this reads the current rule first and merges ``updates``\n  onto it before writing \u2014 only the keys you supply change.\n- Takes effect immediately on matching traffic.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_traffic_rule(rule_id=\"65f...\", updates={\"action\": \"ALLOW\"})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "rule_id": {
            "description": "The ``_id`` from ``list_traffic_rules``.",
            "type": "string"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial traffic-rule record to merge. Common keys:\n``action``, ``matching_target``, ``target_devices``,\n``enabled``, plus target-specific fields.",
            "type": "object"
          }
        },
        "required": [
          "rule_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_traffic_rule"
    },
    {
      "description": "Patch fields on an existing VLAN/network record.\n\nSide effects:\n- Modifies the named network in place. Only fields supplied in\n  ``updates`` change; everything else is preserved.\n- Changes to ``vlan`` or ``ip_subnet`` may disconnect clients on the\n  affected network.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_vlan(network_id=\"65f...\", updates={\"enabled\": False})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "network_id": {
            "description": "The ``_id`` from ``list_networks``.",
            "type": "string"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial network record. Common keys: ``name``, ``vlan``,\n``ip_subnet``, ``dhcpd_start``, ``dhcpd_stop``, ``enabled``,\n``mdns_enabled`` (toggle the per-VLAN mDNS reflector\nindependently of network creation), ``purpose``.",
            "type": "object"
          }
        },
        "required": [
          "network_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_vlan"
    },
    {
      "description": "Patch fields on an existing WiFi SSID.\n\nSide effects:\n- Modifies the named WLAN in place. Only fields supplied in\n  ``updates`` change; everything else is preserved.\n- Changes to ``security``, ``wpa_mode``, or ``x_passphrase`` may\n  disconnect connected clients until they re-authenticate.\n- Passphrases passed via ``x_passphrase`` are redacted in the\n  response.\n- Mutates controller state. Use dry_run=True to preview the change\n  without applying.\n\nExample: update_wlan(wlan_id=\"65f...\", updates={\"enabled\": False})",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "controller": {
            "default": "default",
            "description": "Name of the UniFi controller to target. Defaults to\n``\"default\"``.",
            "type": "string"
          },
          "dry_run": {
            "default": false,
            "description": "Preview the change without applying it. Returns the\npredicted change set.",
            "type": "boolean"
          },
          "updates": {
            "additionalProperties": true,
            "description": "Partial WLAN record. Common keys: ``name``, ``enabled``,\n``x_passphrase``, ``wpa_mode``, ``hide_ssid``, ``wlan_band``,\n``is_guest``.",
            "type": "object"
          },
          "wlan_id": {
            "description": "The ``_id`` from ``list_wlans``.",
            "type": "string"
          }
        },
        "required": [
          "wlan_id",
          "updates"
        ],
        "type": "object"
      },
      "name": "update_wlan"
    }
  ],
  "version": 1
}
